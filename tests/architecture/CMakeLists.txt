cmake_minimum_required(VERSION 3.25)
project(ArchitectureRejection LANGUAGES NONE)
include("${CMAKE_CURRENT_LIST_DIR}/../../cmake/Architecture.cmake")
add_library(vision_contracts INTERFACE)
add_library(vision_application INTERFACE)
add_library(vision_inspection INTERFACE)
add_library(vision_runtime INTERFACE)
add_library(vision_ipc INTERFACE)
add_library(vision_frame_transport INTERFACE)
add_library(vision_plugin_sdk INTERFACE)
add_library(vision_plugin_runtime INTERFACE)
add_library(vision_capture INTERFACE)
add_library(vision_algorithm INTERFACE)
add_library(vision_inference INTERFACE)
add_library(vision_replay INTERFACE)
add_library(vision_storage INTERFACE)
add_library(vision_modbus INTERFACE)
add_library(Qt6::Core INTERFACE IMPORTED)
if(NOT DEFINED PROTECTED_TARGET)
    set(PROTECTED_TARGET vision_application)
endif()
if(NOT DEFINED FORBIDDEN_DEPENDENCY)
    set(FORBIDDEN_DEPENDENCY Qt6::Core)
endif()
target_link_libraries(${PROTECTED_TARGET} INTERFACE ${FORBIDDEN_DEPENDENCY})
vision_check_architecture()
