add_executable(vision-core-tests core_test.cpp)
add_executable(vision-readiness-tests readiness_test.cpp)
target_link_libraries(vision-readiness-tests PRIVATE vision_application vision_doctest)
vision_configure_target(vision-readiness-tests)
add_test(NAME application.production_readiness COMMAND vision-readiness-tests)
target_link_libraries(vision-core-tests PRIVATE vision_application vision_doctest)
vision_configure_target(vision-core-tests)
add_test(NAME core.build_identity COMMAND vision-core-tests)
add_executable(vision-framing-tests framing_test.cpp)
target_link_libraries(vision-framing-tests PRIVATE vision_ipc vision_doctest)
vision_configure_target(vision-framing-tests)
add_test(NAME ipc.framing COMMAND vision-framing-tests)
add_executable(vision-ipc-channel-tests ipc_channel_test.cpp)
target_link_libraries(vision-ipc-channel-tests PRIVATE vision_ipc vision_application vision_serialization vision_json vision_doctest)
vision_configure_target(vision-ipc-channel-tests)
add_test(NAME ipc.channel COMMAND vision-ipc-channel-tests)
add_executable(vision-supervisor-tests supervisor_test.cpp)
target_link_libraries(vision-supervisor-tests PRIVATE vision_runtime vision_doctest)
vision_configure_target(vision-supervisor-tests)
add_test(NAME runtime.supervisor COMMAND vision-supervisor-tests)
add_executable(vision-scheduler-tests scheduler_test.cpp)
target_link_libraries(vision-scheduler-tests PRIVATE vision_runtime vision_doctest)
vision_configure_target(vision-scheduler-tests)
add_test(NAME runtime.scheduler COMMAND vision-scheduler-tests)
add_executable(vision-buffer-tests buffer_test.cpp)
target_link_libraries(vision-buffer-tests PRIVATE vision_runtime vision_serialization vision_json vision_doctest)
vision_configure_target(vision-buffer-tests)
add_test(NAME runtime.buffer COMMAND vision-buffer-tests)
add_executable(vision-capture-tests capture_test.cpp)
target_link_libraries(vision-capture-tests PRIVATE vision_capture vision_doctest)
vision_configure_target(vision-capture-tests)
add_test(NAME capture.source COMMAND vision-capture-tests)
add_executable(vision-algorithm-tests algorithm_test.cpp)
target_link_libraries(vision-algorithm-tests PRIVATE vision_algorithm vision_doctest)
vision_configure_target(vision-algorithm-tests)
add_test(NAME algorithm.brightness COMMAND vision-algorithm-tests)
set_tests_properties(ipc.channel PROPERTIES TIMEOUT 20)
find_package(Threads REQUIRED)
add_executable(vision-foundation-tests foundation_test.cpp)
target_link_libraries(vision-foundation-tests PRIVATE vision_application vision_serialization vision_json vision_doctest Threads::Threads)
target_compile_definitions(vision-foundation-tests PRIVATE "VISION_SOURCE_DIR=\"${PROJECT_SOURCE_DIR}\"")
vision_configure_target(vision-foundation-tests)
add_test(NAME core.foundation COMMAND vision-foundation-tests)
set_tests_properties(core.foundation PROPERTIES TIMEOUT 30)
if(WIN32)
    add_executable(vision-storage-tests storage_test.cpp)
    target_link_libraries(vision-storage-tests PRIVATE vision_storage vision_sqlite vision_application vision_inference vision_doctest)
    vision_configure_target(vision-storage-tests)
    add_test(NAME storage.transaction COMMAND vision-storage-tests)
    add_executable(vision-replay-tests replay_test.cpp)
    target_link_libraries(vision-replay-tests PRIVATE vision_replay vision_inference vision_doctest)
    target_compile_definitions(vision-replay-tests PRIVATE "VISION_MODEL_DIR=L\"${PROJECT_SOURCE_DIR}/examples/models\"")
    vision_configure_target(vision-replay-tests)
    vision_deploy_ort(vision-replay-tests)
    add_test(NAME replay.assets COMMAND vision-replay-tests)
    add_executable(vision-inference-tests inference_test.cpp)
    target_link_libraries(vision-inference-tests PRIVATE vision_inference vision_json vision_doctest)
    target_compile_definitions(vision-inference-tests PRIVATE "VISION_MODEL_DIR=L\"${PROJECT_SOURCE_DIR}/examples/models\"")
    vision_configure_target(vision-inference-tests)
    vision_deploy_ort(vision-inference-tests)
    add_test(NAME inference.detection COMMAND vision-inference-tests)
endif()
add_test(NAME cli.contract COMMAND "${CMAKE_COMMAND}"
    "-DCLI=$<TARGET_FILE:vision-cli>" "-DEXPECTED_VERSION=${PROJECT_VERSION}"
    -P "${CMAKE_CURRENT_SOURCE_DIR}/cli_test.cmake")
add_test(NAME architecture.reject_qt COMMAND "${CMAKE_COMMAND}"
    "-DFIXTURE=${CMAKE_CURRENT_SOURCE_DIR}/architecture"
    "-DBINARY=${CMAKE_CURRENT_BINARY_DIR}/architecture-rejection"
    -P "${CMAKE_CURRENT_SOURCE_DIR}/architecture_test.cmake")
foreach(pair IN ITEMS "vision_inspection,vision_runtime" "vision_runtime,vision_inspection" "vision_contracts,Qt6::Core" "vision_ipc,Qt6::Core"
    "vision_frame_transport,Qt6::Core" "vision_plugin_sdk,Qt6::Core" "vision_plugin_runtime,vision_runtime"
    "vision_capture,vision_runtime" "vision_algorithm,vision_runtime" "vision_inference,Qt6::Core" "vision_replay,vision_application"
    "vision_storage,Qt6::Core" "vision_modbus,vision_application")
    string(REPLACE "," ";" parts "${pair}")
    list(GET parts 0 protected)
    list(GET parts 1 forbidden)
    add_test(NAME "architecture.${protected}" COMMAND "${CMAKE_COMMAND}"
        "-DFIXTURE=${CMAKE_CURRENT_SOURCE_DIR}/architecture"
        "-DBINARY=${CMAKE_CURRENT_BINARY_DIR}/reject-${protected}"
        "-DPROTECTED_TARGET=${protected}" "-DFORBIDDEN_DEPENDENCY=${forbidden}"
        -P "${CMAKE_CURRENT_SOURCE_DIR}/architecture_test.cmake")
endforeach()
if(VISION_BUILD_LOCAL_IPC)
    add_executable(vision-process-host-tests process_host_test.cpp)
    target_link_libraries(vision-process-host-tests PRIVATE vision_runtime_qt)
    vision_configure_target(vision-process-host-tests)
    if(WIN32)
        add_executable(vision-http-output-tests http_output_test.cpp)
        add_executable(vision-durable-agent-tests durable_agent_test.cpp)
        add_executable(vision-frame-archive-probe frame_archive_probe.cpp)
        vision_configure_target(vision-frame-archive-probe)
        add_executable(vision-archive-pipeline-tests archive_pipeline_test.cpp)
        vision_demo_resources(vision-archive-pipeline-tests)
        target_link_libraries(vision-archive-pipeline-tests PRIVATE vision_storage vision_sqlite vision_inference vision_serialization vision_json)
        vision_configure_target(vision-archive-pipeline-tests)
        add_dependencies(vision-archive-pipeline-tests vision-storage-tool vision-frame-archive vision-frame-archive-probe vision-durable-output-agent vision-file-output vision-sqlite-output)
        foreach(scenario IN ITEMS normal ng unknown pause stop restart endurance startfail hang crash overflow malformed corrupt locked trace-missing)
            add_test(NAME "archive.pipeline.${scenario}" COMMAND vision-archive-pipeline-tests "${scenario}"
                "$<TARGET_FILE_DIR:vision-sqlite-output>/sqlite-output.json" "$<TARGET_FILE_DIR:vision-file-output>/file-output.json"
                "$<TARGET_FILE:vision-durable-output-agent>" "$<TARGET_FILE:vision-frame-archive>" "$<TARGET_FILE:vision-frame-archive-probe>" "$<TARGET_FILE:vision-storage-tool>")
            set_tests_properties("archive.pipeline.${scenario}" PROPERTIES TIMEOUT 30)
        endforeach()
        add_executable(vision-durable-demo-tests durable_demo_test.cpp)
        vision_demo_resources(vision-durable-demo-tests)
        target_link_libraries(vision-durable-demo-tests PRIVATE vision_storage vision_sqlite vision_inference vision_serialization)
        vision_configure_target(vision-durable-demo-tests)
        add_dependencies(vision-durable-demo-tests vision-durable-output-agent vision-file-output vision-sqlite-output)
        foreach(scenario IN ITEMS normal ng unknown corrupt locked outputfail pause stop stop-pending restart initfail agentfail endurance)
            add_test(NAME "demo.durable.${scenario}" COMMAND vision-durable-demo-tests "${scenario}"
                "$<TARGET_FILE_DIR:vision-sqlite-output>/sqlite-output.json" "$<TARGET_FILE_DIR:vision-file-output>/file-output.json"
                "$<TARGET_FILE:vision-durable-output-agent>")
            set_tests_properties("demo.durable.${scenario}" PROPERTIES TIMEOUT 30)
        endforeach()
        vision_demo_resources(vision-durable-agent-tests)
        target_link_libraries(vision-durable-agent-tests PRIVATE vision_storage vision_json)
        vision_configure_target(vision-durable-agent-tests)
        add_dependencies(vision-durable-agent-tests vision-durable-output-agent vision-file-output)
        add_test(NAME storage.durable_agent COMMAND vision-durable-agent-tests "$<TARGET_FILE:vision-durable-output-agent>"
            "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-file-output>/file-output.json")
        set_tests_properties(storage.durable_agent PROPERTIES TIMEOUT 40)
        foreach(scenario IN ITEMS failed expired mixed)
            add_test(NAME "storage.durable_agent.${scenario}" COMMAND vision-durable-agent-tests "$<TARGET_FILE:vision-durable-output-agent>"
                "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-file-output>/file-output.json" "${scenario}")
            set_tests_properties("storage.durable_agent.${scenario}" PROPERTIES TIMEOUT 40)
        endforeach()
        vision_demo_resources(vision-http-output-tests)
        target_include_directories(vision-http-output-tests PRIVATE "${VISION_NATIVE_ROOT}/include")
        target_link_libraries(vision-http-output-tests PRIVATE vision_plugin_runtime vision_storage vision_json ws2_32 crypt32)
        vision_configure_target(vision-http-output-tests)
        add_dependencies(vision-http-output-tests vision-http-output)
        add_test(NAME output.http.contract COMMAND vision-http-output-tests "$<TARGET_FILE_DIR:vision-http-output>/http-output.json")
        set_tests_properties(output.http.contract PROPERTIES TIMEOUT 15)
        add_executable(vision-network-output-process-tests network_output_process_test.cpp)
        vision_demo_resources(vision-network-output-process-tests)
        vision_configure_target(vision-network-output-process-tests)
        add_dependencies(vision-network-output-process-tests vision-http-output)
        foreach(scenario IN ITEMS normal slow)
            add_test(NAME "output.http.process.${scenario}" COMMAND vision-network-output-process-tests
                "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-http-output>/http-output.json"
                "$<TARGET_FILE_DIR:vision-test-output>/test-output.json" "${scenario}")
            set_tests_properties("output.http.process.${scenario}" PROPERTIES TIMEOUT 12)
        endforeach()
        add_executable(vision-file-output-tests file_output_test.cpp)
        add_executable(vision-file-output-process-tests file_output_process_test.cpp)
        vision_demo_resources(vision-file-output-process-tests)
        target_link_libraries(vision-file-output-process-tests PRIVATE vision_inference)
        vision_configure_target(vision-file-output-process-tests)
        add_dependencies(vision-file-output-process-tests vision-file-output)
        add_test(NAME output.file.write_failure_isolation COMMAND vision-file-output-process-tests
            "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-file-output>/file-output.json"
            "$<TARGET_FILE_DIR:vision-test-output>/test-output.json")
        set_tests_properties(output.file.write_failure_isolation PROPERTIES TIMEOUT 12)
        vision_demo_resources(vision-file-output-tests)
        target_link_libraries(vision-file-output-tests PRIVATE vision_plugin_runtime vision_inference vision_json)
        vision_configure_target(vision-file-output-tests)
        add_dependencies(vision-file-output-tests vision-file-output)
        foreach(format IN ITEMS jsonl csv)
            add_test(NAME "output.file.${format}" COMMAND vision-file-output-tests "$<TARGET_FILE_DIR:vision-file-output>/file-output.json" "${format}")
            set_tests_properties("output.file.${format}" PROPERTIES TIMEOUT 10)
        endforeach()
        add_executable(vision-persistent-output-tests persistent_output_test.cpp)
        vision_demo_resources(vision-persistent-output-tests)
        target_link_libraries(vision-persistent-output-tests PRIVATE vision_storage)
        vision_configure_target(vision-persistent-output-tests)
        add_dependencies(vision-persistent-output-tests vision-sqlite-output)
        foreach(scenario IN ITEMS normal corrupt)
            add_test(NAME "storage.output.${scenario}" COMMAND vision-persistent-output-tests "$<TARGET_FILE:vision-worker-host>"
                "$<TARGET_FILE_DIR:vision-sqlite-output>/sqlite-output.json" "$<TARGET_FILE_DIR:vision-test-output>/test-output.json" "${scenario}")
            set_tests_properties("storage.output.${scenario}" PROPERTIES TIMEOUT 12)
        endforeach()
        add_executable(vision-modbus-device-tests modbus_device_test.cpp)
        vision_demo_resources(vision-modbus-device-tests)
        vision_configure_target(vision-modbus-device-tests)
        add_dependencies(vision-modbus-device-tests vision-modbus-device)
        foreach(scenario IN ITEMS normal fragment timeout disconnect old_ack wrong_transaction)
            add_test(NAME "modbus.device.${scenario}" COMMAND vision-modbus-device-tests "$<TARGET_FILE:vision-worker-host>"
                "$<TARGET_FILE_DIR:vision-modbus-device>/modbus-device.json" "${scenario}")
            set_tests_properties("modbus.device.${scenario}" PROPERTIES TIMEOUT 10)
        endforeach()
        add_executable(vision-model-tool-tests model_tool_test.cpp)
        target_link_libraries(vision-model-tool-tests PRIVATE vision_application_qt)
        vision_demo_resources(vision-model-tool-tests)
        vision_configure_target(vision-model-tool-tests)
        add_dependencies(vision-model-tool-tests vision-model-tool)
        foreach(scenario IN ITEMS detection classification segmentation invalid replay physical)
            add_test(NAME "model.tool.${scenario}" COMMAND vision-model-tool-tests "$<TARGET_FILE:vision-model-tool>"
                "${PROJECT_SOURCE_DIR}/examples/models" "${scenario}")
            set_tests_properties("model.tool.${scenario}" PROPERTIES TIMEOUT 25)
        endforeach()
        add_executable(vision-onnx-session-tests onnx_session_test.cpp)
        target_link_libraries(vision-onnx-session-tests PRIVATE vision_application_qt)
        vision_configure_target(vision-onnx-session-tests)
        add_dependencies(vision-onnx-session-tests vision-worker-host vision-sim-camera vision-onnx-algorithm)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-onnx-session-tests POST_BUILD COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake"
                "$<TARGET_FILE:Qt6::${component}>" "$<TARGET_FILE_DIR:vision-onnx-session-tests>")
        endforeach()
        add_test(NAME inference.session COMMAND vision-onnx-session-tests "$<TARGET_FILE:vision-worker-host>"
            "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json" "$<TARGET_FILE_DIR:vision-onnx-algorithm>/onnx-algorithm.json"
            "${PROJECT_SOURCE_DIR}/examples/models/detection.json")
        set_tests_properties(inference.session PROPERTIES TIMEOUT 18)
        add_test(NAME inference.classification.session COMMAND vision-onnx-session-tests "$<TARGET_FILE:vision-worker-host>"
            "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json" "$<TARGET_FILE_DIR:vision-onnx-algorithm>/onnx-algorithm.json"
            "${PROJECT_SOURCE_DIR}/examples/models/classification.json")
        set_tests_properties(inference.classification.session PROPERTIES TIMEOUT 18)
        add_test(NAME inference.classification.uncertain COMMAND vision-onnx-session-tests "$<TARGET_FILE:vision-worker-host>"
            "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json" "$<TARGET_FILE_DIR:vision-onnx-algorithm>/onnx-algorithm.json"
            "${PROJECT_SOURCE_DIR}/examples/models/classification-uncertain.json")
        set_tests_properties(inference.classification.uncertain PROPERTIES TIMEOUT 18)
        add_test(NAME inference.segmentation.session COMMAND vision-onnx-session-tests "$<TARGET_FILE:vision-worker-host>"
            "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json" "$<TARGET_FILE_DIR:vision-onnx-algorithm>/onnx-algorithm.json"
            "${PROJECT_SOURCE_DIR}/examples/models/segmentation.json")
        set_tests_properties(inference.segmentation.session PROPERTIES TIMEOUT 18)
        add_executable(vision-recipe-runtime-tests recipe_runtime_test.cpp)
        vision_demo_resources(vision-recipe-runtime-tests)
        vision_configure_target(vision-recipe-runtime-tests)
        foreach(scenario IN ITEMS switch rollback rollback-fail save-fail)
            add_test(NAME "recipe.runtime.${scenario}" COMMAND vision-recipe-runtime-tests "${scenario}")
            set_tests_properties("recipe.runtime.${scenario}" PROPERTIES TIMEOUT 18)
        endforeach()
        add_executable(vision-demo-recipe-tests demo_recipe_test.cpp)
        vision_demo_resources(vision-demo-recipe-tests)
        vision_configure_target(vision-demo-recipe-tests)
        add_test(NAME demo.recipe COMMAND vision-demo-recipe-tests)
        set_tests_properties(demo.recipe PROPERTIES TIMEOUT 15)
        foreach(scenario IN ITEMS normal ng algorithm-crash output-hang camera-missing)
            add_test(NAME "demo.station.${scenario}" COMMAND vision-station-demo "${scenario}")
            set_tests_properties("demo.station.${scenario}" PROPERTIES TIMEOUT 20)
        endforeach()
        add_executable(vision-dispatcher-tests dispatcher_test.cpp)
        target_link_libraries(vision-dispatcher-tests PRIVATE vision_application_qt)
        vision_configure_target(vision-dispatcher-tests)
        add_dependencies(vision-dispatcher-tests vision-worker-host vision-test-output)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-dispatcher-tests POST_BUILD COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake"
                "$<TARGET_FILE:Qt6::${component}>" "$<TARGET_FILE_DIR:vision-dispatcher-tests>")
        endforeach()
        foreach(scenario IN ITEMS normal hang crash lost_ack fail wrong_ack capacity restore_pending restore_accepted restore_acked expired bad_snapshot)
            add_test(NAME "output.dispatcher.${scenario}" COMMAND vision-dispatcher-tests "${scenario}"
                "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-test-output>/test-output.json")
            set_tests_properties("output.dispatcher.${scenario}" PROPERTIES TIMEOUT 15)
        endforeach()
        add_executable(vision-device-session-tests device_session_test.cpp)
        target_link_libraries(vision-device-session-tests PRIVATE vision_application_qt)
        vision_configure_target(vision-device-session-tests)
        add_dependencies(vision-device-session-tests vision-worker-host vision-sim-device vision-sim-camera vision-basic-algorithm)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-device-session-tests POST_BUILD COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake"
                "$<TARGET_FILE:Qt6::${component}>" "$<TARGET_FILE_DIR:vision-device-session-tests>")
        endforeach()
        foreach(scenario IN ITEMS normal edges pause lost_ack old_ack disconnect hang crash safety lease)
            add_test(NAME "device.session.${scenario}" COMMAND vision-device-session-tests "${scenario}"
                "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-sim-device>/sim-device.json"
                "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json" "$<TARGET_FILE_DIR:vision-basic-algorithm>/basic-algorithm.json")
            set_tests_properties("device.session.${scenario}" PROPERTIES TIMEOUT 15)
        endforeach()
        add_executable(vision-station-tests station_test.cpp)
        target_link_libraries(vision-station-tests PRIVATE vision_application_qt)
        vision_configure_target(vision-station-tests)
        add_dependencies(vision-station-tests vision-worker-host vision-sim-camera vision-basic-algorithm)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-station-tests POST_BUILD COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake"
                "$<TARGET_FILE:Qt6::${component}>" "$<TARGET_FILE_DIR:vision-station-tests>")
        endforeach()
        foreach(scenario IN ITEMS normal ng pause timeout missing crash cancel duplicate out_of_order capacity)
            add_test(NAME "application.station.${scenario}" COMMAND vision-station-tests "${scenario}"
                "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json"
                "$<TARGET_FILE_DIR:vision-basic-algorithm>/basic-algorithm.json")
            set_tests_properties("application.station.${scenario}" PROPERTIES TIMEOUT 15)
        endforeach()
        add_executable(vision-algorithm-session-tests algorithm_session_test.cpp)
        target_link_libraries(vision-algorithm-session-tests PRIVATE vision_algorithm_qt)
        vision_configure_target(vision-algorithm-session-tests)
        add_dependencies(vision-algorithm-session-tests vision-worker-host vision-sim-camera vision-basic-algorithm)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-algorithm-session-tests POST_BUILD COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake"
                "$<TARGET_FILE:Qt6::${component}>" "$<TARGET_FILE_DIR:vision-algorithm-session-tests>")
        endforeach()
        foreach(scenario IN ITEMS normal ng invalid timeout cancel crash isolation recovery)
            add_test(NAME "algorithm.session.${scenario}" COMMAND vision-algorithm-session-tests "${scenario}"
                "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json"
                "$<TARGET_FILE_DIR:vision-basic-algorithm>/basic-algorithm.json")
            set_tests_properties("algorithm.session.${scenario}" PROPERTIES TIMEOUT 15)
        endforeach()
        add_test(NAME capture.demo COMMAND vision-capture-demo "$<TARGET_FILE:vision-worker-host>"
            "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json")
        set_tests_properties(capture.demo PROPERTIES TIMEOUT 15)
        add_executable(vision-capture-session-tests capture_session_test.cpp)
        target_link_libraries(vision-capture-session-tests PRIVATE vision_capture_qt vision_capture)
        vision_configure_target(vision-capture-session-tests)
        add_dependencies(vision-capture-session-tests vision-worker-host vision-sim-camera)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-capture-session-tests POST_BUILD COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake"
                "$<TARGET_FILE:Qt6::${component}>" "$<TARGET_FILE_DIR:vision-capture-session-tests>")
        endforeach()
        foreach(scenario IN ITEMS normal replay timeout cancel crash disconnect duplicate out_of_order isolation restart recovery backpressure pixel-hash)
            add_test(NAME "capture.session.${scenario}" COMMAND vision-capture-session-tests "${scenario}"
                "$<TARGET_FILE:vision-worker-host>" "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json")
            set_tests_properties("capture.session.${scenario}" PROPERTIES TIMEOUT 15 WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
        endforeach()
        add_executable(vision-camera-tests camera_test.cpp)
        target_link_libraries(vision-camera-tests PRIVATE vision_plugin_runtime vision_capture vision_frame_transport vision_runtime vision_json Qt6::Core)
        vision_configure_target(vision-camera-tests)
        add_dependencies(vision-camera-tests vision-sim-camera)
        add_custom_command(TARGET vision-camera-tests POST_BUILD COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake"
            "$<TARGET_FILE:Qt6::Core>" "$<TARGET_FILE_DIR:vision-camera-tests>")
        foreach(scenario IN ITEMS lifecycle cancel delay missing disconnect duplicate out_of_order process crash replay sequence buffers)
            add_test(NAME "capture.camera.${scenario}" COMMAND vision-camera-tests "${scenario}"
                "$<TARGET_FILE_DIR:vision-sim-camera>/sim-camera.json")
            set_tests_properties("capture.camera.${scenario}" PROPERTIES TIMEOUT 15 WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
        endforeach()
        foreach(plugin IN ITEMS good bad missing)
            add_library(vision-test-plugin-${plugin} SHARED plugins/test_plugin.cpp)
            target_link_libraries(vision-test-plugin-${plugin} PRIVATE vision_plugin_sdk)
            vision_configure_target(vision-test-plugin-${plugin})
        endforeach()
        target_compile_definitions(vision-test-plugin-bad PRIVATE VISION_BAD_API)
        target_compile_definitions(vision-test-plugin-missing PRIVATE VISION_MISSING_FACTORY)
        add_executable(vision-plugin-tests plugin_test.cpp)
        target_link_libraries(vision-plugin-tests PRIVATE vision_plugin_runtime vision_runtime_qt)
        vision_configure_target(vision-plugin-tests)
        add_dependencies(vision-plugin-tests vision-worker-host vision-test-plugin-good vision-test-plugin-bad vision-test-plugin-missing)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-plugin-tests POST_BUILD
                COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake" "$<TARGET_FILE:Qt6::${component}>"
                "$<TARGET_FILE_DIR:vision-plugin-tests>")
        endforeach()
        foreach(scenario IN ITEMS loader normal crash hang isolation cancel init-fail execute-fail oversize bad-result orphan stale-parent destroy-fail lease freeze)
            add_test(NAME "plugin.${scenario}" COMMAND vision-plugin-tests "${scenario}" "$<TARGET_FILE:vision-worker-host>"
                "$<TARGET_FILE:vision-test-plugin-good>" "$<TARGET_FILE:vision-test-plugin-bad>" "$<TARGET_FILE:vision-test-plugin-missing>")
            set_tests_properties("plugin.${scenario}" PROPERTIES TIMEOUT 15 WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
        endforeach()
        add_executable(vision-mapping-tests mapping_test.cpp)
        target_link_libraries(vision-mapping-tests PRIVATE vision_frame_transport vision_runtime vision_serialization Qt6::Core Psapi)
        vision_configure_target(vision-mapping-tests)
        add_custom_command(TARGET vision-mapping-tests POST_BUILD
            COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake" "$<TARGET_FILE:Qt6::Core>"
            "$<TARGET_FILE_DIR:vision-mapping-tests>")
        foreach(scenario IN ITEMS roundtrip abandon slow functional bandwidth)
            add_test(NAME "frame.mapping.${scenario}" COMMAND vision-mapping-tests "${scenario}")
            set_tests_properties("frame.mapping.${scenario}" PROPERTIES TIMEOUT 30)
        endforeach()
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-process-host-tests POST_BUILD
                COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake" "$<TARGET_FILE:Qt6::${component}>"
                "$<TARGET_FILE_DIR:vision-process-host-tests>")
        endforeach()
        foreach(scenario IN ITEMS normal crash hang no-ready ignore-stop launch-fail lease freeze rotation isolation progress station callbacks
            task-normal task-late task-timeout task-cancel auto-rotation callback-timeouts job)
            add_test(NAME "runtime.host.${scenario}" COMMAND vision-process-host-tests "${scenario}")
            set_tests_properties("runtime.host.${scenario}" PROPERTIES TIMEOUT 12)
        endforeach()
    endif()
    foreach(scenario IN ITEMS roundtrip bad-token partial-header partial-body malformed oversize sequence
        slow-reader silent truncated wrong-version request-timeout disconnect late-response)
        add_test(NAME "ipc.transport.${scenario}" COMMAND vision-ipc-probe "${scenario}")
        set_tests_properties("ipc.transport.${scenario}" PROPERTIES TIMEOUT 10)
    endforeach()
    add_executable(vision-ipc-local-test ipc_local_test.cpp)
    target_link_libraries(vision-ipc-local-test PRIVATE vision_ipc Qt6::Core Qt6::Network)
    vision_configure_target(vision-ipc-local-test)
    if(WIN32)
        foreach(component IN ITEMS Core Network)
            add_custom_command(TARGET vision-ipc-local-test POST_BUILD
                COMMAND ${CMAKE_COMMAND} -P "${PROJECT_SOURCE_DIR}/cmake/CopyRuntime.cmake" "$<TARGET_FILE:Qt6::${component}>"
                "$<TARGET_FILE_DIR:vision-ipc-local-test>")
        endforeach()
    endif()
    add_test(NAME ipc.local_process COMMAND vision-ipc-local-test)
    add_test(NAME ipc.reject_token COMMAND vision-ipc-local-test --reject-token)
    set_tests_properties(ipc.local_process ipc.reject_token PROPERTIES TIMEOUT 10)
endif()
if(VISION_BUILD_GUI)
    if(VISION_BUILD_LOCAL_IPC)
        add_test(NAME gui.model-tool COMMAND vision-workstation --model-smoke)
        set_tests_properties(gui.model-tool PROPERTIES TIMEOUT 25)
    endif()
    add_test(NAME gui.offscreen COMMAND vision-workstation --smoke-test)
    set_tests_properties(gui.offscreen PROPERTIES TIMEOUT 15)
    if(VISION_BUILD_LOCAL_IPC)
        foreach(scenario IN ITEMS normal ng algorithm-crash output-hang camera-missing close stop
            durable durable-ng durable-stop durable-close durable-restart)
            add_test(NAME "gui.pipeline.${scenario}" COMMAND vision-workstation --pipeline-smoke "${scenario}")
            set_tests_properties("gui.pipeline.${scenario}" PROPERTIES TIMEOUT 20)
        endforeach()
    endif()
endif()

find_package(Python3 COMPONENTS Interpreter REQUIRED)
add_test(NAME tooling.skill_scripts COMMAND "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tests/skill_scripts_test.py")
set_tests_properties(tooling.skill_scripts PROPERTIES TIMEOUT 30)
