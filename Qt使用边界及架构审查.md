# Qt 使用边界及开发计划架构审查

审查日期：2026-09-18  
审查对象：《开发计划及要求.md》0.1 与此前架构讨论。  
证据范围：文档审查、Qt 官方许可页面与 LGPLv3 原文；没有框架实现可供运行验证。  
结论：可以保留总体方向，开始基础工程与领域契约工作；生产相关执行和恢复协议需要先补齐。不能将本文理解为法律合规认证或生产稳定性认证。

## 1. 当前是否采用 Qt 商业授权

最新实施追踪（2026-09-19，文档0.11）：T22～T27批次增加SQLite、libcurl、cpp-httplib、
Modbus TCP、存储及持久输出工具。Qt仍仅使用既有Core/Network/Gui/Widgets动态库，
未增加Qt模块或商业授权。存储和HTTP插件不链接Qt；持久分发进程使用既有Qt适配层。
依赖来源继续使用本地artifacts.lock.json及开发环境已保存许可证；自有许可证尚未选定。
本批硬件、完整恢复编排及正式发行验收未关闭；以下条目为历史记录。

最新实施追踪（2026-09-19，文档0.10）：T16～T21增加CPU ONNX、掩码引用、Qt模型工具及回放资产。
插件及纯推理/资产库不依赖Qt；应用适配层仍只使用既有Qt Core/Network/Gui/Widgets动态库，
未增加Qt模块或购买商业授权。ONNX Runtime固定本地1.30.0 SDK；自有项目许可证仍待选择。
本次验证是Windows开发范围，不替代T31发行许可证资料与干净环境验收。
A04/A06设计由[ADR-012](docs/decisions/ADR-012-恢复与可靠分发边界.md)关闭；
生产数据库事务和真实设备仍待对应任务。以下为历史批次记录。

最新实施追踪（2026-09-19，文档0.8）：[ADR-010](docs/decisions/ADR-010-正式采集工作流.md)接通Camera公共宿主与采集协调适配层；后者使用既有Qt Core/Network，不增加商业模块，图源/插件仍无Qt依赖。[VAL-012](docs/acceptance/VAL-20260919-012-正式双相机采集链路.md)通过全部回归。A04/A06和正式发行合规检查仍未关闭；下方为此前批次追踪。

最新实施追踪（2026-09-19）：[ADR-009](docs/decisions/ADR-009-模拟采集分步接入与全量回归.md)及[VAL-011](docs/acceptance/VAL-20260919-011-全量复核与模拟采集源.md)落实T10a图源和Camera DLL，采集源和插件均未引入Qt。Camera公共宿主业务接入待T10b；A04/A06仍未关闭。当前Qt技术依赖边界未扩大，此条不是重新进行法律审查。

前批实施追踪（2026-09-19）：[ADR-008](docs/decisions/ADR-008-共享图像与插件宿主边界.md)及[VAL-010](docs/acceptance/VAL-20260919-010-共享图像与插件宿主验收.md)完成Windows共享图像、插件SDK和Algorithm公共宿主基础；没有新增Qt商业模块，纯C++ SDK不依赖Qt。公共宿主启动孤儿保护已验证，其他插件业务适配及产线/长稳仍待后续验收。

前批实施追踪（2026-09-19）：[ADR-007](docs/decisions/ADR-007-监护健康与调度边界.md)落实A03/A07设计修订，Windows健康租约、双实例、监护与调度见[VAL-009](docs/acceptance/VAL-20260919-009-监护与调度验收.md)。PLC就绪撤销和真实设备旧会话接管待T13/T23。下方早期记录保留原审查语境。

第二批追踪：ADR-004/005 已将 Windows 优先、组合预算、早期诊断/测试和任务拆分写回主计划0.3；A05/A08/A09 完成设计整合，A03/A04/A06/A07 仍待处理。验证见 VAL-006，未新增 Qt 商业模块。

实施追踪（2026-09-18）：A01/A02 已分别通过 [ADR-001](docs/decisions/ADR-001-Qt与依赖边界.md)、[ADR-002](docs/decisions/ADR-002-首版模拟设计范围.md) 写回主计划 0.2；Windows 骨架验证见 [VAL-004](docs/acceptance/VAL-20260918-004-工程骨架.md)。下文保留原始审查语境，A03～A09 尚未关闭，不能把本批实现当作全部问题解决。

**原计划没有选定或购买 Qt 商业授权，也没有规定必须购买。** 第 3.3 节把 Qt 及依赖选型留在 T01，第 23.2 节仅要求评估动态链接和核对许可。因此，准确状态是“Qt 技术方向已作为默认，授权与模块清单尚未冻结”，不是“已经获得商业授权”。

本次建议将开发默认路线明确为：

> 使用提供 LGPLv3 许可选项的 Qt 模块，采用开源 LGPLv3 路线和动态链接，履行对应分发义务；本框架默认构建不得要求 Qt 商业授权。自有代码许可证独立选择，建议评估 Apache-2.0 或 MIT。具体 Qt 版本、构建来源与分发材料在 T01 固定。

这是建议冻结的方案；本文没有替维护者购买授权、签署协议或选择最终自有代码 LICENSE。

## 2. Qt 的技术使用边界

| 位置 | 建议 Qt 使用规则 | 原因 |
|---|---|---|
| `src/inspection` | 不依赖 Qt，不使用 QString、QObject、QVariant 等公共类型 | 领域规则可以无 UI 独立测试 |
| `src/runtime` | 核心策略与状态不依赖 Qt；通过时钟、进程、通道端口工作 | 更换基础设施不重写调度语义 |
| `src/contracts` | 独立 C++ 值类型与序列化模式 | 避免 Qt 类型贯穿全部模块 |
| `src/application` | 用例与公共接口不依赖 Qt；Qt 事件桥接放外围 | UI/CLI 复用同一逻辑 |
| `src/plugin-sdk` | 默认不在公共插件接口暴露 Qt 对象或容器 | 减少 Qt 版本与线程亲和性耦合；不意味着 C++ ABI 自动稳定 |
| `src/ui`、`apps/workstation` | 可以使用 Qt Core、Gui、Widgets | 窗口、控件、图像交互和操作入口 |
| `src/ipc` 的 Qt 传输实现、平台适配、worker 入口 | 可以使用 Qt Core、Network；协议和值类型与 Qt 分开 | QLocalSocket、进程和事件循环属于实现手段 |
| 相机、ONNX、S3、输出插件 | 默认纯 C++；确需 Qt 时逐项申报模块 | 不因项目用了 Qt 就要求所有插件都用 Qt |

需要把原目录中的 IPC 协议代码与 Qt transport 实现拆成不同 CMake target；文件夹分开而 target 全部链接 Qt，仍然不满足该边界。

工作进程使用 QCoreApplication 并不表示它有 GUI。反过来，去掉 Qt 窗口也不表示它不再依赖 Qt。

### 2.1 建议模块白名单

| 模块 | 本项目用途 | 本次官方页面确认的许可选择 | 开源默认构建建议 |
|---|---|---|---|
| Qt Core | 事件循环、外围基础设施 | 商业许可或包含 LGPLv3 的开源选项 | 允许，按 LGPLv3 履约 |
| Qt Gui | 图像、绘制与图形支持 | 商业许可或包含 LGPLv3 的开源选项 | UI 使用 |
| Qt Widgets | 桌面控件 | 商业许可或包含 LGPLv3 的开源选项 | UI 使用 |
| Qt Network | 本地套接字等通信基础设施 | 商业许可或包含 LGPLv3 的开源选项 | 允许，不把它等同于 Qt HTTP Server |
| Qt HTTP Server | HTTP 服务 | 商业许可或 GPLv3 | 默认不采用，选择单独核对许可的 HTTP 库 |
| Qt MQTT | MQTT 客户端 | 商业许可或 GPLv3 | 默认不采用，选择单独核对许可的 MQTT 库 |
| 其他 Qt 模块 | 暂无必要依赖 | 本次未逐项核对 | 默认不加入，经过许可及架构检查后添加 |

依据 [Q1]～[Q7]。白名单不是“永远只能用这四个模块”，而是新增模块必须有实际用途并核对选定版本许可证，不能凭“Qt 是开源的”全部引入。

标准 Qt 平台插件及其第三方依赖也需要进入发布清单，不能只列直接链接的四个模块。

## 3. 别人拿框架商用怎么处理

Qt 的 LGPL 路线允许满足其条件的应用保持闭源；**收费或商业使用本身并不自动要求购买 Qt 商业授权**。Qt 官方义务说明明确区分了这些选择。[Q2]

需要分开检查两层：

1. 本项目自有代码的许可证允许下游做什么。原计划的 MIT/Apache-2.0 是候选，尚未落定；不能把候选当成已经发布的授权。
2. 下游实际交付物包含的 Qt、模型、SDK 和其他组件各要求什么。框架自己的许可证不能代替第三方授权。

### 3.1 默认 LGPL 路线需要落实的内容

- 随分发物提供 Qt 使用声明，以及适用的 LGPL/GPL 许可文本。
- 针对实际分发的 Qt 库，履行对应源码提供义务，包含适用修改和构建相关材料；具体提供方式按许可证及实际分发方式安排。
- 使用符合 LGPLv3 要求的共享库链接机制；应用能与接口兼容的修改版库一起运行。
- 不通过应用许可条款禁止 LGPL 所保护的库修改及为调试这些修改所需的逆向工程。
- 在适用时提供安装信息，让用户能安装、运行修改后重新组合的版本；这不是无条件要求公开所有设备密钥，适用范围需按 LGPLv3 第 4 条及 GPLv3 第 6 条判断。
- 为 Qt 自带第三方代码及实际加载的平台插件保留各自的许可材料。

依据 [Q1]、[Q2]、[Q8]。**动态链接是较便于履约的工程路线，不是只放几个 DLL 就完成合规。**

静态链接不是一律禁止，但对应重新组合/链接等义务更复杂，首版不把它作为默认发行路线。商业授权适用于选择商业条款的情况，具体覆盖范围仍由合同决定。

### 3.2 对工控交付尤其要检查的地方

如果下游把程序装进锁定设备，通过签名或完整性校验阻止替换 Qt 库，需要评估其是否阻碍适用的 LGPL 权利。安全校验与许可要求应一起设计，不能简单写成“禁止替换任何 DLL”。

本框架可限制自己插件的版本兼容性，但不能把插件安全策略不加区分地套到 LGPL 库替换权利上。

商业客户是否需要购买 Qt 授权，应依据其具体模块、分发与设备限制判断，不能由本框架统一承诺“永远不用买”或“只要商用就要买”。

## 4. 架构审查结论

总体方向合理：

- 运行内核、检测模型、应用协调分开。
- 插件扩展与进程隔离分开。
- 图像通路与控制消息分开。
- 检测结论与各输出的交付状态分开。
- 无硬件演示与生产模式分开。

主要缺口是：**若干核心协议还停留在原则层面，足以规划模块，不足以让不同开发者或 AI 独立实现后可靠拼接。**

下面的 P1 表示涉及生产正确性或会造成核心返工，应在对应模块实现前修订；P2 表示必须纳入开发或发布门槛，但不阻止所有基础工作。它们是文档设计风险，不是已复现的代码缺陷。

## 5. P1：主控卡死时，PLC 通信宿主可能继续表示正常

依据：开发计划第 4.1、7.1、8.3、12.3 节。文档规定工作进程在控制通道失联后退出，也规定 PLC 心跳，但没有完整规定“主控未退出、连接未断、应用事件线程不再推进”的处理。

场景：主控应用线程死锁，device-worker 仍正常读写 PLC；如果它独立刷新上位机心跳并保持 Ready，PLC 可能继续送料，而检测协调已经停止。

修订要求：

- 定义由生产协调状态推进提供的 `control_lease`，含 run_id、会话、递增序号和有效期。
- 不允许仅由一个无关的后台保活线程代表生产协调器更新健康。
- device-worker 在租约过期时撤销就绪、停止接纳相关命令；如果它无法通信，由 PLC 自身的上位机失联策略接管。
- PLC 层应约定心跳失效、就绪失效、结果超时各自的现场处置。
- 正在等待异步工作不等于卡死；健康表示控制循环可推进，工件期限另行监测。

验收新增：保持 socket 和 device-worker 存活，仅冻结主控应用事件线程；验证 PLC 就绪在约定期限内失效。实际物理安全仍依赖现场硬件与 PLC 设计。

落点：T06/T07/T13，不能等到最后长稳阶段。

## 6. P1：“结果已持久”与“可靠输出已入队”之间缺少恢复协议

依据：第 7.3、15.2、16.3 节。

文档正确承认输出收到 DurablyQueued 前不能声称可靠；但没有完整说明这个窗口发生主控崩溃后，是明确放弃该通道，还是从持久化结果恢复投递。

场景：检测结果已提交 SQLite，主控在向可靠输出宿主投递前崩溃。输出自己的 outbox 中没有记录，仅扫描该 outbox 无法找回。

建议默认设计：

- 对配置为可靠异步的输出，在保存权威结果时同一数据库事务保存 `dispatch_intent(event_id, output_instance_id, route_version)`。
- 独立调度恢复这些意图，非阻塞提交给输出宿主。
- 输出宿主以 `event_id + output_instance_id` 唯一去重，持久接纳后返回 DurablyQueued。
- ACK 丢失可再次提交相同身份，不创建新业务事件。
- 只有明确接纳后才推进分发意图状态；保存路由版本和停用实例的处置规则。
- 尽力展示不需要这一持久保证。
- PLC 的时效性命令不能按上述数据补发流程恢复，必须独立现场对账。

这不要求跨数据库分布式事务，也不承诺恰好一次。它补的是“谁负责从结果存储到输出 outbox 的那一段”。

验收新增：在结果事务提交后、发送前，以及输出提交后、ACK 前分别杀死主控，验证可靠数据最终可恢复或有明确不可恢复状态。

落点：T14 与 T24 共同定义契约。

## 7. P1：容量预留没有覆盖一次工件的多个依赖

依据：第 7.3、8.5、10.2 节。当前只说明成功预留后接纳，未写清跨相机、算法和必需存储的组合接纳。

场景：相机 A 有槽位，相机 B 已满，算法还有队列但必需存图空间不足。如果各模块分别先占资源再等其他资源，会出现半接纳、资源滞留或实际已经拍照却无处保存。

修订要求：

- 一个工件计算完整预算，创建 `AdmissionReservation`。
- 按固定顺序、非阻塞地申请所需资源；任何一步失败，释放已获预留并拒绝或撤销就绪。
- 远端存储提供有期限的预留票据，带宿主 epoch；宿主重启时票据失效。
- 明确预留、提交、过期、撤销、实际使用五种行为及幂等性。
- 必需存图预留采用格式/编码上限或可靠的保守预算，不按平均压缩率承诺。
- 硬件已触发时使用事先约定的接纳窗口和容量余量；软件拒绝不能撤销已发生的物理拍照。

无需建设通用分布式事务平台；有限资源票据和补偿释放即可，但必须写完整。

验收新增：第二相机或存储拒绝后，第一相机资源不会永久占用；重启和票据超时后容量最终一致。

落点：T04/T08/T12/T24。

## 8. P1：主控重启后的在途记录和物理交付对账不完整

依据：第 7.2、7.3、15.2 节。

文档已有“不重发旧控制、不自动生产”和运行会话表，但没有规定上次运行残留的 Pending/Running 检测如何结束，也未明确 PLC 已处理但本地 ACK 状态尚未保存的处理。

建议补一个恢复矩阵：

| 恢复时发现 | 处理 |
|---|---|
| 旧 run 有未终结检测 | 保留历史，记为中断，质量 Unknown；不可恢复成继续运行 |
| 已有权威完成结果但图片 Pending | 保持质量结果不变，恢复图像上传 |
| 可靠外部数据输出未确认 | 按持久分发意图恢复 |
| PLC 交付已发出、确认未知 | 标记 Unknown，与现场核对，禁止无条件重发 |
| 配方/模型资产缺失 | 阻止启用相关配方 |
| 旧宿主或旧共享池仍存活 | 隔离并确认退出，不重复接管设备 |

不一定新增所有枚举，但至少要有 `reason=HostRestartInterrupted` 等稳定原因，不能把崩溃中断混成普通用户取消。

验收新增：在开始登记、采图、算法返回、结果落库、PLC ACK 各边界重启，检查历史状态和现场动作。

落点：T12/T23/T24。

## 9. P1：同机多个主控及旧实例的设备所有权没有防护契约

依据：第 3.2 节的“唯一连接所有者”和第 8.3 节的新 run_id。它们解决进程内规划及旧消息，但不自动防止用户启动两份主控。

场景：两份主控分别创建自己的 device-worker，使用不同 run_id，但同时写同一个 PLC；仅靠不同运行 ID 不会阻止外部设备动作。

修订要求：

- 以稳定工位/设备身份获取 OS 级独占启动锁，处理启动竞争。
- 锁释放不等于旧工作进程和外部设备会话已结束；恢复前验证旧宿主。
- 工作宿主绑定父主控身份与会话，不接受其他运行实例命令。
- PLC 协议支持时增加会话/控制权校验，拒绝旧会话命令。
- 首版默认拒绝第二份主控接管同一工位；不同工位是否允许并行启动写入部署策略。

验收新增：同时启动两份程序，第二份明确拒绝控制同一工位；旧宿主未结束时新主控不得直接开始生产。

落点：T06/T13/T23。

## 10. P1：生产约束确认得太晚，可能导致前期架构选错

依据：第 3.3 节把节拍、期限和相机数量最迟确定在 T30；T30 已是性能和长稳验证阶段。

这不适合作为完整工程的开发顺序。线扫或面阵、每件帧数、相机路数、是否等待存图、结果截止时间，会影响内存池、任务模型和存储方案。

修订要求：

- T01 前后定义一份首版“设计包络”：例如限定面阵 2D、有限相机路数与尺寸范围、每件帧数上限、触发模式、目标节拍范围。
- 没有真实客户参数时可以先选明确的参考设备档位，但标为开发目标，不作现场承诺。
- 原有 1024×768 模拟基准仅作功能基准，增加接近目标分辨率/带宽的压力基准。
- 共享内存、调度和存储大规模实现前，先完成最小吞吐/延迟实验。
- T30 用来验证已建立的约束，不是第一次决定这些约束。

落点：T01、T04、T08 之前增加设计包络与数据通路实验。

## 11. P2：Qt 替换边界目前只限制 UI，不够精确

依据：第 3.1、3.2、5.1 节。

领域不依赖 Qt 已明确，但 contracts、application、plugin-sdk 和 IPC 协议自身能否用 Qt 公共类型没有固定。若 AI 在所有接口中使用 QObject、QString 和 QVariant，未来更换 UI 仍可能需要大范围重写。

修订：采用本文第 2 节的 target 边界；在 T01/T02 固定依赖图，T09 固定插件类型及创建销毁规则。禁 Qt 规则不能只靠代码评审记忆。

不需要因此把所有外围 Qt 代码改成自研。QLocalSocket 等可以保留在独立适配 target 中。

## 12. P2：可观测性、资源约束和故障测试集中得太晚

依据：T28 才集中建设日志/指标/进程资源策略，T29 才集中故障矩阵；前文虽要求早期验证，任务交付物未充分统一。

修订：

- T02 建立关联日志字段和错误规范，T06 能看到生命周期，T08 能看到租约和槽位。
- 每个模块同步交付自己的故障测试，T29 做整机回归，不首次补测。
- 软件配额与 OS 强制限制区分：队列上限、进程内存上限、CPU 约束、磁盘配额并非同一种机制。
- 不支持强制限额的平台明确降级为监测/告警/终止；不能宣称仅靠检查 RSS 就能避免系统 OOM。
- GPU 显存预算和驱动故障保留实测边界。

长稳“内存增长不超过 10%”只能作初步门槛，不能证明无泄漏；同时关注增长斜率、句柄、线程、磁盘和租约余额。

## 13. P2：任务粒度仍偏大，公共契约要先冻结

T08 同时包含共享内存平台适配、租约、多消费者和崩溃回收；T24 同时包含 SQLite、暂存、S3 和对账。这些适合作为里程碑，不适合单次 AI 编码任务。

建议拆分：

- T08a：单生产者/单消费者池与布局验证。
- T08b：多消费者租约及幂等释放。
- T08c：超时、崩溃、旧 epoch 与孤儿池回收。
- T24a：元数据及分发意图事务。
- T24b：有限本地暂存与恢复。
- T24c：真实 S3/RustFS 上传。
- T24d：部分成功对账与保留清理。

每个子任务先给出协议示例、状态表和失败用例，再让 AI 编码。不是要求一次补完所有最终代码签名，而是避免关键所有权规则由不同实现者临场猜测。

## 14. 可以后续完善、不需要现在推翻的部分

- UI 与主控是否完全分进程：当前已明确接受同进程边界，若首版没有无界面持续生产要求，不必立即拆分。
- HTTP 与存储查询是否进一步独立读模型：先按有限查询配额压测，确有争用再拆，不先建设完整 CQRS 平台。
- 复杂 DAG/流程编辑器：保留后续计划。
- 大量品牌适配、MQ 和 MES 页面：不因本次审查增加首版范围。
- 插件 C ABI：固定工具链 C++ 接口可以先用，必须有严格 ABI 和销毁规则；无需为了泛化立即改造全部接口。

## 15. 建议修订顺序

1. 冻结 Qt 开源路线、模块白名单和依赖边界。
2. 提前确定首版设计包络，增加高数据量通路实验。
3. 补主控健康租约、唯一控制者和恢复矩阵。
4. 补组合接纳票据，以及可靠输出的持久分发意图。
5. 把日志、资源诊断和故障测试前移，拆分 T08/T24 等任务。

T01 的最小构建、纯领域模型和协议 Schema 可以先做；关键进程恢复、生产接纳与可靠输出的主体实现，应在相关契约补齐后展开。

本次未直接重写原计划中的生产协议，避免把审查建议混成已经确认的设计决定。本文作为修订依据保存。

## 16. 官方来源与核验说明

本次网页搜索工具没有返回可用正文；随后通过 HTTPS 读取以下官方页面，核对许可段落和 LGPLv3 第 4 条。模块结论基于查询时的 Qt 6 文档，不替代最终固定版本源码包中的 LICENSE 文件。

- [Q1] Qt Licensing：`https://doc.qt.io/qt-6/licensing.html`
- [Q2] Qt 开源许可义务：`https://www.qt.io/development/open-source-lgpl-obligations`
- [Q3] Qt Core：`https://doc.qt.io/qt-6/qtcore-index.html`
- [Q4] Qt Gui：`https://doc.qt.io/qt-6/qtgui-index.html`
- [Q5] Qt Widgets：`https://doc.qt.io/qt-6/qtwidgets-index.html`
- [Q6] Qt Network：`https://doc.qt.io/qt-6/qtnetwork-index.html`
- [Q7] Qt HTTP Server：`https://doc.qt.io/qt-6/qthttpserver-index.html`；Qt MQTT：`https://doc.qt.io/qt-6/qtmqtt-index.html`
- [Q8] LGPLv3 原文：`https://www.gnu.org/licenses/lgpl-3.0.html`

许可证部分只说明本次核对的通用条件和项目建议；实际商业分发涉及设备限制、安装方式和合同，应针对交付物再审查。
