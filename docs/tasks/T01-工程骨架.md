# T01：第一批正式工程骨架

日期：2026-09-18。父任务状态：已完成（Windows 阶段，ADR-004）。用户已明确要求后移 Linux 验证。

## 本批工作

1. A01：Qt 与依赖边界冻结，ADR-001 和主计划同步。
2. A02：双模拟相机设计范围，ADR-002 和 profile 文件。
3. 根 CMake、Windows core/gui 与 Linux core presets、明确 target 依赖。
4. 纯 C++ application/contract 构建身份接口，CLI 明确报告尚未具备检测能力。
5. 可选 Qt 外壳，显示版本和未实现状态，不提供生产启动按钮。
6. doctest、CLI 契约、架构负向检查、GUI 离屏运行。
7. README、忽略规则、CI 定义和验收/日志。

不实现 T02 工件身份协议、T03 质量规则、相机、PLC 或多进程闭环。A03～A09 仍按对应前置任务处理。

## 验收

- `node tools/build.cjs windows-core` 不依赖 Qt，构建与 CTest 通过。
- `node tools/build.cjs windows-gui` 使用固定 Qt 版本并通过离屏测试。
- 核心非法链接 Qt 的小型 fixture 被准确拒绝，不能把任何配置错误都算成功。
- CLI 未实现的生产命令退出非零，不虚报 Ready。
- Git 忽略 SDK、参考仓库、构建文件，但保留正式源码与文档。
- Linux CI 定义可提交；未在 Linux 执行就保持父任务进行中。

本批使用已确认设计及 brainstorming 的职责/边界检查；本机不存在额外 writing-plans 技能文件，因此使用既有第 21～22 章与本任务卡实施，无需扩大范围。

验收记录：`docs/acceptance/VAL-20260918-004-工程骨架.md`。

2026-09-18 收尾历史：依赖决策完成，但 WSL2 无法启动（VAL-005）。此后用户明确要求 Windows 优先，ADR-004 将 Linux 移至 V-LINUX；T01 按现行 Windows 门槛关闭，T02～T04 已由 VAL-006 验收。Linux 未执行的事实保持不变。
