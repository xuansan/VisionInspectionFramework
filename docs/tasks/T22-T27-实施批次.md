# T22～T27实施批次

2026-09-19，用户授权继续到T27，每阶段windows-all，结束再次从头全回归。
沿用已批准架构，硬件验收不以协议模拟代替。

- T22：本机无MVS SDK；官方公开目录返回5.1.0 Windows条目，安装包下载返回HTML拦截。
  原始证据out/mvs-catalog.json、开发环境/下载包/MVS/download-blocked.html。
  不伪造头文件或ABI；SDK接入/实拍/拔线/重连保持待验证，不阻塞其他任务。
- T23：限定Modbus TCP IPv4，显式端口/unit/寄存器映射；独立Communication插件。
  session+command/result序号回显、读实际PLC状态、过期撤销Ready由PLC看门狗负责。
  使用本地真实TCP协议对端测试分包/异常/旧ACK/超时，现场PLC仍待验证。
- T24：独立存储代码与进程接口，SQLite单写、结果/intent同事务，
  限额暂存、确定性对象键、libcurl SigV4 RustFS实接，上传读取hash核对。
  数据库忙/满/损坏、重复/冲突、恢复和对象故障逐项验证。
- T25：独立文件输出DLL，JSONL/固定CSV、大小/数量限制、完整记录及公式转义。
- T26：独立HTTP输出DLL，仅只读REST/SSE，令牌来自环境，有限结果窗口、
  连接/请求/缓冲和超时；OpenAPI与实现一致；图片不可伪装Available。
- T27：生产接纳门槛和持久确认状态，未满足硬件/追溯/控制权时拒绝Production；
  UI显示各依赖与交付状态，不能单凭“数据库能写”开启真实动作。

不引入低代码、MES、MQ等超出本批次的功能。每个未完成的必需项如实列明；
硬件缺失不降低父任务完成定义。

批次实际结果：见VAL-20260919-024及开发进度台账0.11。
T23、T25、T26模块软件已有通过证据；T24、T27仅部分完成，
包括SQLite事务/交付租约、独立清算工具、RustFS上传续传、生产准入策略与界面缺项说明。
T22官方SDK受阻且插件未实现。完整生产编排、图像租约入库、追溯/对象对账及真实硬件仍待完成。
上述计划中的“忙/满/恢复”已做SQLite与软件限额/进程级测试，不等同整机磁盘占满或断电实测。
