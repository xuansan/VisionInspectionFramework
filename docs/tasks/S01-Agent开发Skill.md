# S01：框架使用者开发Skill首版

2026-09-20。用户确认前一轮方案并授权制作。共五步：
1. 核对接口与增加可选源码集成接入点。
2. 实现模拟上位机、ResultOutput插件两个可构建模板。
3. 编写Skill、按需参考资料、检查/生成/验证脚本。
4. 在独立业务目录运行模板；验证不覆盖已有文件、版本不匹配、真实故障路径。
5. 执行框架全部回归，记录证据、剩余限制及使用方法。

Skill位于.agents/skills/vision-framework-dev，与仓库一同维护；不修改全局Agent配置。
公共库、插件宿主及既有进程边界复用，业务工程通过VISION_EXTENSION_DIR接入。
接入为源码集成，不假装已有install/export SDK；生成器不会复制/改写框架内核。
首版Windows x64、MSVC v143动态CRT、Release、Qt6.8.3；不宣称跨编译器ABI兼容。

验收：新业务目录构建GUI模拟；插件经独立宿主加载，正常/拒绝/丢ACK和健康输出隔离；
脚本无覆盖、路径与命名校验、版本/接口指纹检查；结构验证及全量旧测试。
新会话Agent行为评估与提速效果需后续实用验证，不由脚本通过替代。
