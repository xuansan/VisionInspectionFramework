# T16～T21实施批次

2026-09-19，用户授权按既定计划推进，每阶段完成后运行windows-all全量回归。
Windows/CPU优先；生产、真实硬件、GPU、Linux仍不在本批次验收范围。

1. T16：把Demo启动配方解析抽成可复用快照服务；严格验证、内容hash、
   QSaveFile原子替换、保留最后可用版本。运行切换排空旧工件和worker，
   新宿主全部Ready后激活；部分失败停新宿主，重建旧版本，回退失败保持Faulted。
2. T17：固定CPU ONNX Runtime构建，目标检测适配契约、预处理/NMS/坐标恢复，
   真正加载ONNX模型并通过独立参考数值测试、坏模型和超时隔离测试。
3. T18：分类适配、softmax/标签/阈值契约及参考验证。
4. T19：实例分割、系数/原型解码、有限掩码与坐标还原、参考IoU验证。
5. T20：有界图像预览、参数/模型试运行，Qt界面和运行端均验证配置。
6. T21：不可变模型资产、配方与图像回放，独立Replay入口禁止物理/生产输出。

设计选择：沿用小型应用服务和现有worker，不引入低代码平台或新分布式层。
优先固定明确导出契约；不声称支持所有YOLO导出布局。测试模型必须来源明确、
生成可复现且输入相关，参考结果不得直接调用待测C++后处理作为“参考”。
逐阶段记录实际范围、失败与修复、完整测试证据，不能把占位接口标成完成。
