# VAL-20260919-013：T11基础算法

- Windows MSVC x64 Release，Qt6.8.3；代码变更见ADR-011。
- 开发前全量通过：`out/validation/full-2026-09-19T08-57-05-801Z`。
- 首轮失败保留：`out/validation/full-2026-09-19T09-06-28-667Z`。
  新消息遗漏Session白名单导致8项算法场景失败；架构夹具遗漏新target导致依赖测试失败。
- 修复后全量通过：`out/validation/full-2026-09-19T09-09-43-175Z/summary.json`。
  核心20/20、GUI110/110、IPC109/109（配置间重复，不相加）；
  原有Schema/CLI、18类消息72项正反校验、图像及相机manifest检查均通过。

新增：纯算法闭区间/整数均值/padding/颜色/无效布局/取消测试；
原子读租约移交、旧凭据/过期/退出清算；
8项实际Camera→Algorithm进程场景：正常连续3张、NG、旧代次拒绝、
timeout、cancel、crash、isolation、recovery。各场景旁路健康链完成6次检测。
recovery用新池/epoch重新进入配置的崩溃场景，不宣称故障插件已自愈。

算法独立DLL只读共享像素；没有Qt/SDK/ONNX依赖。异常时先逻辑终结再等待退出，
故障池退役；算法适配层析构只有确认OS退出才解除读租约。
任务范围完成不代表无潜在bug；硬件、长稳、节拍、Linux/GPU未测。
