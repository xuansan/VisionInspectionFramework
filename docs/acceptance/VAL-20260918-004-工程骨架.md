# VAL-20260918-004：工程骨架验收

日期：2026-09-18。关联 A01、A02、T01、LOG-20260918-008。执行者：Codex。本地文件验收，无 Git 提交号。

## 环境与范围

Windows 11 x64；VS2022 Community 17.12.4；MSVC 19.42.34436.0；Windows SDK 10.0.22621.0；CMake 3.29.5-msvc4；Qt 6.8.3 MSVC2022 x64；doctest 2.5.3；Release。

只验证工程骨架、构建信息、CLI 行为、依赖边界和 Qt 无屏幕窗口创建，不验证工业检测能力。

## 命令与结果

工作目录为仓库根目录：

```powershell
node tools/build.cjs windows-core
node tools/build.cjs windows-gui
```

| 项目 | 实际结果 | 本机原始日志目录 |
|---|---|---|
| 纯 C++ 配置/编译/CTest | 通过，3/3 | `out/validation/2026-09-18T09-59-09-520Z/` |
| Qt 配置/编译/CTest（修正后） | 通过，4/4 | `out/validation/2026-09-18T10-01-36-746Z/` |
| Qt 连续第二次增量构建 | 通过，4/4 | `out/validation/2026-09-18T10-01-39-241Z/` |

每个目录包含 configure.log、build.log、test.log、vswhere.log。out 属于本机产物，不纳入源码。

- `core.build_identity`：构建身份值正确，检测能力明确为 false。
- `cli.contract`：版本和状态可查询；未实现的生产启动命令返回非零退出码。
- `architecture.reject_qt`：故意让 application 链接 Qt6::Core；必须得到指定的依赖边界错误，不能把任意配置失败当作通过。
- `gui.offscreen`：Qt 窗口在 offscreen 平台构建并渲染后退出，不启动设备、不进行生产控制；不代表交互体验验收。

补充静态检查：`dumpbin /dependents` 检查核心 CLI，仅见 MSVC/C 运行库和 KERNEL32，没有 Qt DLL；三个 JSON 文件解析通过；9 份本批相关 Markdown 的本地链接全部存在。在 `out/ignore-check` 临时仓库用 `git check-ignore` 验证 SDK、out、reference 示例路径被忽略，正式源码和 README 保留；没有初始化项目根仓库或创建提交。

## 失败、处理与限制

GUI 首次构建通过，但重复配置在本机 CMake 中以 3221226505 退出。失败记录保留在 `out/validation/2026-09-18T09-59-11-209Z/`、`2026-09-18T09-59-39-597Z/` 和 `2026-09-18T10-00-41-283Z/`。

跟踪停止于 Qt 的部署支持设置路径；清除配置缓存后首次可通过，重复仍失败。现阶段没有调用 Qt 安装期部署 API，因此在查找 Qt 前设置 `QT_SKIP_SETUP_DEPLOYMENT=ON`，继续使用工程自己的 DLL/平台插件复制。修正后连续两次构建通过。这是固定 Qt 版本下的局部规避，不声明查明 CMake 内部根因；该内部开关在升级 Qt 和 T31 打包时必须重新评估。

诊断期间直接在沙箱内重新检测编译器失败，符合此前 MSBuild/FileTracker 限制；正式构建通过已授权的本机执行完成。未修改全局 PATH、Qt SDK 源文件或系统 DLL。

没有 Vulkan 头文件的提示未阻止本批 Widgets 构建；本批不提供 Vulkan 功能。

Linux CI 文件已建立但未触发；没有 Linux、完整 Debug、GPU、真实相机、PLC、共享内存、多进程故障隔离、ONNX 或长稳验证结果。A01/A02 只完成设计冻结，模拟配置尚无执行器。T01 父任务继续保持“进行中”。
