# T21安全回放与T16～T21批次收尾

2026-09-19。T21首轮完整成功：
`out/validation/full-2026-09-19T11-34-05-127Z/summary.json`，
核心22/22、GUI172/172、IPC163/163及Schema全部通过。
11-32-18首次构建发现JSON与数值比较的编译兼容问题，改为显式类型校验/取值；
失败批次保留，成功批次为完整重跑。

收尾增强：推理/回放库编译依赖白名单及禁止Qt/应用依赖测试；
相机交付像素hash不匹配必须失败且健康相机继续；
CheckFinished的错误任务关联和超量掩码必须拒绝。
11-39-40全量核心24/24、GUI175/175、IPC166/166与Schema通过。
截图检查发现小图缩放造成框线遮挡，改为固定屏幕线宽；
ONNX DLL与Qt DLL统一复制锁。
最终`out/validation/full-2026-09-19T11-43-30-039Z/summary.json`：
核心24/24、GUI175/175、IPC166/166及Schema/CLI13、IPC92、Frame7检查全部通过。
三个配置重复用例不相加；全部旧测试保留。
截图`out/validation/model-tool-T21.png`已实际查看，固定细线框、参数和结果均正常显示。

实现：

- `vision_replay`生成内容寻址快照，包含模型、参数/标签/ROI、图像和版本契约。
- 新建写入、manifest最后写，已有包只校验、不覆盖修补；不是断电事务存储。
- 显式`--replay manifest expected_hash output`校验全部内容。
- `ReplayService`复用TrialSession，固定文件相机+ONNX宿主；无PLC、
  Dispatcher、ProductionController、生产计数器。
- 标准CheckResult外层明确Replay；新run与旧运行隔离，不改原结果。
- 模型worker验证实际加载字节，文件相机验证实际交付像素。

测试：相同资产复用、错hash、缺配置、坏模型、篡改图像拒绝；
同快照重跑质量/类别/框/分数一致，run不同；请求物理输出字段拒绝。
三类实际模型、低置信度、掩码容量和全部历史故障/取消测试继续通过。
Qt模型工具已离屏启动、渲染并保存截图检查。

边界：本地CLI资产包不是数据库历史查询或RustFS。
外部管理员仍可改文件，API不可变与hash检测不等于操作系统防篡改。
临时掩码由会话负责清理，CLI持久目录由调用者管理；
Windows CPU原创模型不代表外部训练权重、GPU、Linux或真实生产性能验收。
