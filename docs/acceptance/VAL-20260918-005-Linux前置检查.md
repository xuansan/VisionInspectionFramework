# VAL-20260918-005：Linux 前置检查与依赖基线整理

日期：2026-09-18。关联 T01、I06、LOG-20260918-009。

## 实际结果

Linux 构建未执行，前置环境检查失败。不是 C++ 编译失败，也不能算跨平台验收通过。

| 检查 | 结果 |
|---|---|
| `wsl --list --verbose` | 存在 Ubuntu 和 docker-desktop，均为 WSL2，Stopped |
| `wsl -d Ubuntu -- true` | 失败：`HCS_E_HYPERV_NOT_INSTALLED` |
| Docker 服务 | 未运行；其 Linux 后端同样尚不可用 |
| Win32_Processor.VirtualizationFirmwareEnabled | True |
| Win32_ComputerSystem.HypervisorPresent | False |
| VirtualMachinePlatform 组件状态 | 查询要求管理员提升，当前未确认 |
| 启动配置 | bcdedit 查询返回参数错误，未取得有效证据 |

WSL 原始可读输出保存在 `out/validation/linux-preflight/list.log` 和 `launch.log`。初次沙箱检查另遇权限限制，已在获准的本机执行中复核。

现有证据说明虚拟机监控器当前不可用，但不足以断言组件未安装还是启动配置/待重启导致。未修改 Windows 组件、启动配置、BIOS、WSL 发行版或重启电脑。

## 已完成的独立工作

依赖选型整理为 ADR-003；把忽略目录中的来源/版本/哈希迁到可纳入源码的 `dependencies/artifacts.lock.json`。清单共 15 条制品记录，包含 doctest 实际构建使用的单头文件条目；历史下载校验证据沿用原记录，不宣称重新下载或重新获得发布方证明。

## 解除条件

管理员确认并修复 Windows 虚拟化组件/启动状态，按需重启后，`wsl -d Ubuntu -- true` 必须成功。随后检查 Ubuntu 的 C++20 编译器、CMake ≥3.25 和 Ninja，再执行：

```text
cmake --preset linux-core -DVISION_ALLOW_DOWNLOADS=ON
cmake --build --preset linux-core --parallel 2
ctest --preset linux-core
```

也可在经授权的独立 Linux 机器或 CI 中完成同等验收。目前没有配置远程仓库或触发托管 CI。

T01 的依赖决策已补齐，但父任务因 Linux 验证条件不足保持被阻塞。依现有 T02→T01 前置关系，本次不越过门槛开始 T02 实现。
