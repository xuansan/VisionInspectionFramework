# VAL-20260919-016：T14独立输出

三轮完整回归通过，全部包含旧测试：

- `out/validation/full-2026-09-19T09-35-19-420Z`
- `out/validation/full-2026-09-19T09-39-04-263Z`
- 最终：`out/validation/full-2026-09-19T09-41-42-049Z/summary.json`

最终核心20/20、GUI142/142、IPC141/141及Schema全部通过。
22类IPC共88项Schema/CLI检查。另补设备与输出应答关联反例。
新增12项输出进程场景：正常、挂死、崩溃、丢ACK、明确失败、错ACK、
逐路容量、未发恢复、已发恢复、已ACK恢复、过期、畸形快照。

真实ResultOutput DLL在独立公共宿主执行。测试证实健康输出在另一输出挂死期间完成；
各通道容量单独结算；故障交付Unknown/Failed不改变权威结果。
内存Demo outbox一次插入事件和全部intent；恢复保持event/output键并增加attempt，
已ACK不重发，过期不发；重复键/过深JSON/超容量恢复拒绝。
运行期限使用单调时钟，跨启动快照使用UTC并转换为有限剩余时长。

范围：F19完成模拟验收，F26只验证恢复状态机及工作进程故障。
快照没有自动写入生产数据库，不具备断电持久性；真实事务/磁盘故障F26验收在T24。
所有状态明确Demo/Volatile；没有实现HTTP/MQ/文件生产插件。
