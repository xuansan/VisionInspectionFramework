# T20模型试运行与图像工具验收

2026-09-19。最终全量证据：
`out/validation/full-2026-09-19T11-23-37-564Z/summary.json`，
核心21/21、GUI169/169、IPC160/160及全部Schema通过。

工作站新增模型工具入口，独立窗口可选择PNM和模型配置，编辑置信度与ROI，
显示原图、ROI、检测框和分割掩码，滚轮缩放、拖动平移、鼠标像素坐标及RGB。
图像文件读取/PNM验证放在独立工具进程；UI只解码有界返回缓冲。
推理在算法宿主，不在UI线程。输入图像上限4M像素预览/16MiB文件数据，
响应和错误文本有上限；取消终止工具进程并由Windows Job清理其宿主。

离线应用服务复用文件相机与算法Session，完全不创建PLC/Dispatcher/生产计数。
三类模型实际运行、错误阈值在运行端拒绝、Qt窗口自动启动并渲染结果均通过。

11-17-34批次发现多目标并发复制Qt DLL产生文件占用，新增目录级复制锁。
11-22-05批次发现预览工具未链接capture解析库，补齐后全部回归通过。
保留失败记录，不缩减任何旧用例。

范围：参数是模型配置+阈值/ROI的有效配置，不是完整低代码编辑器；
PNM输入用于确定性首版，JPEG/PNG和硬件实时显示可继续扩展。
当前Qt模块仍为既有Core/Network/Gui/Widgets，没有增加商业模块。
