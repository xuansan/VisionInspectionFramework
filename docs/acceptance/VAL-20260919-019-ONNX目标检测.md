# T17 ONNX CPU目标检测验收

2026-09-19。完整回归证据：`out/validation/full-2026-09-19T10-38-55-403Z/summary.json`。
Windows核心、GUI、IPC与三项Schema检查全部通过。首次编译发现ORT类型查询缺少索引，
修复后重新执行全部回归；失败证据保留在10-33-31批次。

新增独立ONNX算法DLL，实际加载已校验SHA256的模型字节，CPU单线程顺序执行与预热。
经共享内存读取真实图像，CheckFinished携带有界检测框、模型hash与测量结果，
由宿主补充可信请求关联身份。Station保留证据。

支持固定float32 NCHW输入和[1,4+C,N]检测输出，无objectness、无内置NMS，
类别值为概率。Mono/RGB/BGR、行padding、ROI、最近邻letterbox、114填充、
类别内NMS、原图坐标裁剪，最多64框。最近邻不是所有YOLO导出的默认预处理，
用户模型必须匹配此明确契约；不声称支持任意YOLO版本。

原始小型输入相关ONNX测试模型，由项目脚本生成，无第三方训练权重。
独立ONNX ReferenceEvaluator生成参考；三种图像灰度对比分数1e-4和坐标，
另测NMS、空结果、ROI、非方形、取消、hash/shape/标签/路径错误。
真实双相机worker进程测试验证证据传递、协议往返与资源回收。
这些测试验证推理与传输契约，不验证生产缺陷识别准确率。
