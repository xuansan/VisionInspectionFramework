# VAL-20260918-007：IPC 基础

日期：2026-09-18 UTC。关联 T05（部分完成）。环境同 VAL-006，Windows x64 Release、Qt 6.8.3 Core/Network。

| 执行 | 结果 | 日志 |
|---|---|---|
| `node tools/build.cjs windows-core` | 8/8 通过 | `out/validation/2026-09-18T23-53-14-463Z/` |
| `node tools/build.cjs windows-gui` | 11/11 通过 | `out/validation/2026-09-18T23-53-18-676Z/` |
| `vision-framing-tests.exe --no-colors` | 5 场景145断言通过 | 可重复运行；包括 framing/session/outbox/pending |

覆盖每个分片边界、连续帧、零长/超长、接收队列满、错误后继续输入、每个不完整字节位置断线；旧epoch/运行身份、错误版本/凭据、未知必需能力、过期握手、重复序号及握手前业务消息拒绝；普通发送容量满仍可接纳停止消息；待请求容量/重复/期限/断线处理。

真实 Qt 本地通信探针启动当前测试程序的独立子进程，使用随机唯一端点、UserAccessOption、有限读缓冲和总测试期限；分两次写入报文并核对回复。另一个进程测试传入错误凭据，确认拒绝连接。测试结束只清理本次创建的子进程，不结束其他程序。

不表示完整 T05 完成：正式 JSON 公共头/业务消息编解码、可复用传输适配器、发送队列与 Qt 内部写缓冲联合限额及半包超时还未实现。当前 Probe 没有恶意客户端安全隔离承诺。F10 仅覆盖封包子集；主控健康租约和工作进程监护尚未实施。

既有 GUI 五种演示回归通过，界面没有新增功能。Linux 按用户要求未处理。
