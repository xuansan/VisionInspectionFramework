# T19实例分割验收

2026-09-19。全量证据：
`out/validation/full-2026-09-19T10-58-59-063Z/summary.json`，
核心21/21、GUI164/164、IPC156/156，三项Schema检查通过。

固定[1,4+C+K,N]和[1,K,Hm,Wm]输出，保留NMS候选索引，
系数乘原型、0.5阈值、最近邻原图坐标映射、检测框裁剪及ROI恢复。
输出张量在预热前验证，最多32原型通道、64实例、16MiB掩码总量。
单掩码1MiB；超限明确失败。取消按图像行检查。

新增MaskReference契约与严格序列化/Schema，仅携带hash、尺寸和实例索引，
像素独立保存到本地临时缓冲，单目录64MiB、4096文件上限。
写入用非阻塞跨进程独占锁维护配额，重用文件必须重新校验hash和尺寸，
空间不足拒绝，不擅自删除被引用的文件。它不是T24持久对象存储。

独立ONNX参考及NumPy原型运算生成掩码，恢复后逐像素IoU=1；
测试ROI外清零、异常膨胀、取消、hash损坏、尺寸不符、配额耗尽与去重。
双相机真实宿主测试验证掩码引用进入标准结果并完成资源清算。
外部训练模型、GPU、生产精度及RustFS上传仍未验收。
