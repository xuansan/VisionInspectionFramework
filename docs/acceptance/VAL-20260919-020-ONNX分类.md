# T18 ONNX分类验收

2026-09-19。最终全量证据：
`out/validation/full-2026-09-19T10-52-04-580Z/summary.json`。
Windows核心21/21、GUI163/163、IPC155/155及全部Schema通过。
首轮10-48-30批次亦通过；增加低置信度进程链路后全部重跑。

固定float32 [1,C]输出，显式logits或probabilities契约。
logits使用减最大值的softmax；概率须有限、范围合法且和接近1。
标签唯一、有界，Top-K排序稳定，阈值以上才输出类别；
显式ok_label控制合格类别，其他类别NG。
阈值未达到时保留分数，返回Failed/Unknown及INFERENCE.LOW_CONFIDENCE，
避免用默认类别或默认OK掩盖不确定性。

独立ONNX ReferenceEvaluator参考，使用1000附近的大logits验证数值稳定性；
三种输入对比分数1e-4、类别、Top-K和未知分支。
真实双相机宿主测试分类证据和低置信度失败经标准结果传递及资源清算。
沿用T17隔离/取消机制，无新增UI或核心依赖。
模型仍为原创契约测试模型，不声明训练质量或现场准确率。
