# 工业视觉框架的优势与开发方式对比

面向读者：有 Java 前后端或普通桌面软件经验，但不熟悉工业视觉与现场设备的软件开发人员。  
配套文档：[架构设计.md](架构设计.md)  
日期：2026-09-15

本文解释为什么要建设这套框架、它解决什么问题，以及这类开发与熟悉的业务系统、桌面工具有什么区别。这里描述的是计划中的架构能力，不表示已有实现或已通过工业现场验证。

## 1. 先说明：上位机并不是一种特殊的界面

上位机通常是运行在电脑或工控机上的软件，通过网络、USB、串口等与现场设备通信，承担配置、监控、数据处理及业务协调。工控机可以先理解为面向现场部署的电脑；它并不会让运行其上的普通程序自动获得硬实时保证。

PLC 是常见的工业控制器，负责现场信号和设备控制。相机、光源、扫码器等提供采集或辅助能力。上位机与这些设备配合，完成一段生产过程。

在本项目中，一次完整工作大致如下：

```text
产品到达检测位置
    ↓
PLC 或硬件发出触发
    ↓
相机拍照，图像与当前工件关联
    ↓
算法识别、分类、分割或测量
    ↓
汇总产品检测结果
    ├── 回传 PLC，参与后续产品处置
    ├── 保存记录与图像，供追溯
    └── 输出到界面、HTTP、物联网或其他系统
```

用户看到的 Qt 窗口只是这个系统的操作入口。窗口后面还有设备状态、数据流、执行期限、资源管理、结果关联及故障恢复。

对于普通工具，“能加载图片并运行模型”可能已经完成主要功能；对于产线检测，还要回答“这张图片属于哪个产品”“结果是否来得及”“没有结果时该产品怎么处理”。

## 2. 与 Java 前后端有哪些相通之处

不应把工业软件理解成与已有软件知识完全不同的领域。接口隔离、消息机制、状态机、幂等、限流、持久化、可观测性和自动化测试仍然适用。差别主要在输入来源、数据规模、物理时序，以及失效之后如何恢复。

| 熟悉的概念 | 本项目中的对应概念 | 不能直接照搬的地方 |
|---|---|---|
| HTTP 请求 / 业务请求 | 一次工件检测触发 | 产品可能一直在移动，不能任意延迟响应 |
| request ID / trace ID | 运行代次、工件 ID、检测任务 ID | 还要保证编号确实关联到现场的物理产品 |
| Service 层 | 应用协调层 | 需要协调设备状态、任务期限和产线就绪 |
| 领域对象 | 工件、检查项、检测结果 | 检测未完成与产品 NG 必须分别表达 |
| 消息队列 | 进程内有界队列、输出通道或外部 MQ | 本地内存队列不天然具备持久化与故障恢复 |
| 服务进程隔离 | 采集、检测、存储与输出工作进程 | 首版主要在一台机器，不等于分布式集群 |
| 超时、重试、熔断 | 任务超时、工作进程恢复、有限重试 | 相机触发、PLC 动作不能无条件重放 |
| 对象存储 | RustFS 保存图像与掩码 | 上传慢不能长期占住采集缓冲 |
| Spring 扩展接口或策略模式 | 相机、算法、输出插件接口 | C++ 动态库还要考虑 ABI、销毁归属和 SDK 线程 |
| Web API | HTTP 查询与 SSE 结果通知 | 网络看板是旁路消费者，不能成为检测完成的必要条件 |

这些对应关系是理解入口，不代表 Spring 接口、操作系统进程和 C++ 插件具有相同的运行边界。

## 3. 工业上位机与普通桌面应用的区别

工业上位机本身也可以是桌面应用。“工业上位机”和“桌面应用”不是互斥分类。下面比较的是产线视觉检测应用与典型的、以人为主要输入来源的桌面工具，并非声称所有普通桌面软件都简单。

医疗、金融、音视频制作等桌面软件也可能有严格的性能与可靠性要求；高负载 Java 后端同样需要流量控制和故障隔离。不能仅依据 UI 形式或编程语言判断技术难度。

| 维度 | 典型桌面工具 | 产线视觉上位机 |
|---|---|---|
| 主要输入 | 用户点击、编辑、导入文件 | 相机连续数据、PLC 触发、现场设备事件 |
| 工作节奏 | 很多操作允许用户等待 | 由设备和产线节拍推动，部分工作有明确截止时间 |
| 处理对象 | 文件、页面或用户操作 | 真实工件及对应图像、检查项和物理位置 |
| 运行模式 | 常见交互式使用，也可能长期运行 | 常见长时间连续生产与有限维护窗口 |
| 失败表现 | 操作失败、未保存、界面失去响应 | 还可能漏检、串件、错误处置或影响生产 |
| 外部依赖 | 文件系统、网络服务、数据库 | 加上相机 SDK、驱动、PLC、触发信号、光源等 |
| 恢复方式 | 用户可能重新打开或重试 | 必须处理在途工件、旧结果、设备状态和现场同步 |
| 验收重点 | 功能、交互、兼容性 | 还要验证节拍、资源上限、异常恢复和追溯一致性 |

区别不在于“工业界面要多几个按钮”，而在于软件参与了一个持续发生的物理过程。

### 3.1 用户可以等待，工件不一定会等待

假设产线每秒通过 10 个产品，相邻产品间隔约 100 ms。这是举例，不是本框架已确定的性能指标。

如果某个产品需要在到达剔除位置前完成判定，结果迟到就可能失去原来的作用。即使算法最终正确识别出缺陷，也不代表系统完成了有效检测闭环。

具体截止时间取决于拍照位置、剔除位置、输送速度和 PLC 跟踪方式，不等于相邻产品间隔。有些系统允许多个工件同时在途，但必须准确关联。

因此需要同时测量：

- 吞吐量：每秒能处理多少件。
- 延迟：一个工件从触发到结果交付要多久。
- 延迟分布：偶发慢任务有多慢，而不只看平均值。
- 在途数量：同时有多少工件未完成。

多开线程可能增加吞吐，也可能造成 CPU/GPU 争用，让尾部延迟更差。线程数不是越多越好。

### 3.2 正确答案还必须属于正确的产品

假设相机 A 检查瓶盖，相机 B 检查标签：

```text
工件 1001 的瓶盖：OK
工件 1002 的标签：OK
```

不能把这两条消息组合成“工件 1001 合格”。两条算法结果单独看都可能完全正确，组合后的业务结论却错误。

这类似后端系统不能把订单 A 的付款记到订单 B，但现场关联还有额外难点：触发可能丢失、相机帧号会变化、工件可能在移动。生成 UUID 并不能自动建立图像与真实产品之间的关系，仍需要与 PLC 或触发机制共同约定。

### 3.3 “没有测出来”不等于“产品不合格”

三种情况必须区分：

| 情况 | 执行状态 | 质量含义 |
|---|---|---|
| 图像和算法正常，发现缺陷 | 已完成 | NG |
| 图像和算法正常，没有发现约定缺陷 | 已完成 | 按规则判 OK |
| 相机没出图或算法崩溃 | 未完成 / 失败 | 没有有效质量结论 |

产线可以选择把未完成检测的产品也剔除，但追溯记录应保留原因。否则设备异常会混入真实不良率，质量分析和设备诊断都会失真。

### 3.4 界面不卡，只是可靠性的一个方面

如果相机断开，界面仍然流畅，却继续显示上一件产品的 OK 并回传给 PLC，这不是可靠系统。

可靠性包含：状态真实、结果不串件、超时可识别、资源有上限、错误有原因、恢复后不使用旧数据。界面响应只是其中一个要求。

## 4. 为什么要做框架，而不是每个项目复制一套代码

从零开发可以完成项目，框架并不是唯一正确选择。建设框架的理由是多个项目会反复遇到同一组难题，而这些难题值得形成统一、可测试的机制。

### 4.1 复用的不只是设备调用代码

最容易想到的是复用海康或大恒相机接口。实际上，以下规则也可以复用：

- 任务从创建到结束的状态转换。
- 图像内存何时可以释放。
- 队列满时如何拒绝任务或请求暂停。
- 进程卡死时如何结束并重新初始化。
- 重复、迟到和旧会话结果如何处理。
- 配方切换时如何保护在途任务。
- 输出插件失败时如何记录并与其他通道隔离。
- 如何用模拟设备测试掉线和恢复。

这些机制比“打开相机、采一张图”的封装更难验证，也更值得集中维护。

### 4.2 一处修复可以惠及多个项目，但需要版本管理

如果每个项目复制一份采集代码，后来发现停止过程中存在缓冲提前释放问题，就需要逐个查找、修改和验证。有些项目可能长期保留旧错误。

共享框架可以让问题在一个实现中修复，并用回归用例覆盖。但客户项目仍需经过版本升级和现场验证，不能假定发布新框架后所有部署会自动安全更新。

框架也会集中风险：公共逻辑的错误可能影响多个项目。因此接口稳定性、发布说明、版本锁定和回滚能力是复用的配套成本。

### 4.3 项目变化有明确的落点

| 变化 | 主要修改位置 | 通常不需要修改 |
|---|---|---|
| 相机品牌替换 | 相机适配插件与配置 | 检测结果分发、HTTP 接口 |
| 更换检测 ONNX 模型 | 模型配置，必要时新增格式适配 | 进程监护和队列机制 |
| 新增 MQTT 发布 | 输出插件与路由配置 | 相机 SDK 和检测判定 |
| 更改缺陷数量判定 | 检查规则与配方 | IPC 协议和对象上传 |
| 改变远程看板 | 外部前端，必要时扩展后端数据契约 | 工业采集执行链路 |

“通常不需要”以原接口能表达新需求为前提。例如面阵相机变成线扫相机，或者算法需要多帧联合输入，可能需要扩展通用契约。框架不能保证所有变化都只改一个插件。

### 4.4 多人协作更容易建立边界

相机开发人员关注设备能力与图像交接；算法人员关注输入输出与模型正确性；平台人员关注执行与恢复；Java 团队可以开发独立的结果消费系统、报表或前端。

各方通过稳定的结果、设备和任务契约协作，减少同时修改一个巨大控制类的情况。接口仍需要共同设计，不是把代码切成几个目录后就能完全互不沟通。

### 4.5 现场排障有统一证据

“偶尔慢一下”可能来自相机丢帧、算法排队、GPU 争用、图像上传或 PLC 应答。没有统一关联信息时，各模块日志很难拼到一起。

框架要求每个阶段记录任务身份、耗时、错误和队列状态，就能检查一个工件究竟停在哪一段。回放配方与样例图像后，可以进一步区分算法问题和现场设备问题。

诊断包还应带 SDK、模型、配置和程序版本；只提供一张报错截图通常不够复现问题。

## 5. 框架开发与每次从零开发：实际取舍

| 项目 | 每次从零实现或复制项目 | 使用统一框架 |
|---|---|---|
| 第一个简单演示 | 可能更快 | 需要先建立基础机制 |
| 第二、第三个类似项目 | 容易继续复制，也逐渐产生差异 | 设备和业务适配可复用已有契约 |
| 稳定性规则 | 各项目分别实现和验证 | 公共机制集中验证，项目适配仍需测试 |
| 新设备与输出 | 容易与界面和主流程耦合 | 有接口时修改位置更明确 |
| 团队协作 | 小团队可能直接沟通即可 | 多团队有统一职责与契约 |
| 现场诊断 | 日志与错误表现可能不同 | 可统一追踪、状态和诊断工具 |
| 复杂度 | 初期低，需求增长后可能集中在大类中 | 初期更高，需要维护边界和工具 |
| 升级风险 | 修改局部项目较直接 | 公共变更需要兼容管理和逐项目验证 |

框架不自动提升算法准确率，不自动解决所有 SDK 兼容问题，也不能消除硬件故障。它提高的是建立一致行为和复用验证结果的能力。

成本收益取决于项目数量、相似度、维护期限、人员变化和故障代价，没有实际工时数据时不承诺固定百分比的效率提升。

## 6. 本框架为什么同时使用模块、插件和多进程

这三个概念解决不同问题。

### 6.1 模块让代码容易理解和修改

代码上区分：

- `runtime`：执行调度、期限、工作进程管理和恢复机制。
- `inspection`：工件、检查项、检测状态与判定规则。
- `application`：将业务需求转换为执行任务并协调结果。
- 插件和基础设施：SDK、推理、通信、存储及输出实现。

可以类比 Java 系统中应用服务、领域模型与适配器分离。但这些代码模块不必分别变成进程。

### 6.2 插件让能力容易替换和扩展

插件通过统一接口提供能力。例如相机插件提供采集，算法插件提供检测，输出插件负责 HTTP 或 MQTT。

插件化不意味着源码不需要维护，也不意味着不同 Qt 或编译器版本的 DLL 一定能互相加载。首版固定工具链并校验接口版本，是更实际的选择。

尤其要注意：主进程加载一个 DLL 后，DLL 与主程序共享地址空间。插件内存越界可能让整个进程崩溃。接口再漂亮也不能改变这个事实。

### 6.3 多进程让部分故障能够被限制

相机 SDK 在采集进程，推理在检测进程，HTTP 在输出进程。相机 SDK 卡住时，主控可以结束并重建采集进程，而不用强行终止主控内部某个持锁线程。

这与后台服务拆进独立进程的理由相通，但本项目首先是同机隔离，并非建设微服务集群。不需要因此引入 Kubernetes、服务注册中心或所有分布式基础设施。

多进程也有代价：IPC、资源回收、状态同步和部署管理更复杂，进程之间仍共享 CPU、内存、磁盘及可能的 GPU。系统级资源耗尽或驱动故障仍可能影响全局。

首版 UI 与主控仍处在同一进程，因此 UI 进程级崩溃会影响主控。这是已说明的范围限制，不宣称当前设计消除了所有单点故障。

## 7. 图像数据为什么需要特别设计

### 7.1 图像不是一条很小的业务 JSON

一张 2448 × 2048 的 Mono8 图像约为 5 MB；如果换成三通道、保留更多位深或中间结果，内存还会增加。

例如四台相机各产生每秒 20 张约 5 MB 的图像，仅原始数据就约为每秒 400 MB。这只是数据量示例，不是目标设备承诺。若这些图像每经过一个模块都复制、编码或通过 JSON/Base64 传递，很快会增加 CPU、带宽和内存压力。

因此建议大图放在共享内存缓冲池，控制通道只传图像位置、大小和身份描述。远程访问的图像经过存储与授权入口获取，不把共享内存指针发到网络上。

### 7.2 共享内存和队列各管一件事

- 共享内存像仓库，多个进程可以访问其中的数据。
- 队列像待处理清单，表示哪些任务还没处理。

仓库里有图，不代表别人知道可以读；任务出队，也不代表图像立刻可以覆盖。必须约定写入、发布、读取和释放的时机。

Java 开发者可以把交接描述理解为资源句柄，但不能照搬 GC 的对象生命周期。厂商 SDK 提供的指针可能只在回调期间有效，跨进程的原始地址也没有直接意义。即使用 C++ 智能指针，仍然需要显式设计跨进程租约和崩溃回收协议。

### 7.3 队列一定要有上限

如果每秒进入 20 个任务，而算法长期只能处理 15 个，队列每秒净增加 5 个。加大队列只延后故障，不会让持续过载消失，还可能让结果越来越迟。

对应的处理方式取决于业务：

| 数据 | 可采取的策略 |
|---|---|
| 预览画面 | 只保留最新帧，旧预览可以覆盖 |
| 正式检测任务 | 拒绝新任务、请求暂停或记录过载，不能静默丢弃 |
| 普通实时看板通知 | 标记缺口、断开慢客户端，允许补查持久化结果 |
| 强制追溯数据 | 容量不满足时按策略暂停接纳，不能悄悄不存 |

后端开发中的背压、限流、容量规划在这里同样适用，只是最终可能需要与产线协同，而不只是返回一个 HTTP 429。

## 8. 超时、重启和重试为什么不能只写一个 catch

### 8.1 超时不代表原操作已经结束

HTTP 请求超时后，服务端事务可能仍在执行，这一点 Java 开发者并不陌生。同样，上位机判定算法超时后，算法线程可能还在读图像。

此时不能立即覆盖缓冲；否则旧算法可能读到下一件产品的数据。框架必须区分逻辑终态与执行资源结束，必要时终止工作进程并确认退出，再回收资源。

进程心跳正常也不能证明算法正常。需要同时检查任务进展和期限。

### 8.2 重启之后还有恢复工作

相机进程重新启动不代表已经可以生产，还需要连接相机、恢复参数、验证就绪、清除旧任务并与产线重新同步。

恢复流程可以概括为：

```text
隔离故障 → 处理在途任务 → 确认停止与回收
    → 有限重启 → 配置恢复 → 就绪验证 → 恢复接纳
```

配置错误不应无限重启。多次恢复失败时应保留故障状态并要求处理。

### 8.3 物理动作不能像普通查询一样重试

向外部查询接口重试通常比较容易控制，但向 PLC 发送一个剔除动作后没收到应答，无法仅凭超时知道动作是否已执行。

这是幂等和不确定结果的问题。需要工件编号、命令编号、应答及设备侧约定；不能因 HTTP 或 MQ 有重试能力，就宣称物理动作恰好执行一次。

历史检测结果可以补发给报表系统，但过期剔除命令不能在新工件到达时补发。

## 9. 用六个项目变化说明框架优势

### 9.1 更换相机品牌

已有设备能力满足需求时，实现新 SDK 适配并验证触发、像素格式和生命周期即可，业务判定和输出接口保持稳定。

复用的价值是隔离改动，不是保证换设备无需测试。不同相机的帧号、曝光、触发方式可能不同，必须重新验证实际采集关联。

### 9.2 更换 YOLO ONNX 模型

通用推理插件管理会话、输入输出和资源；模型适配负责预处理及输出解析。目标检测、分类和实例分割分别产生标准结构化结果。

新模型符合已支持契约时可配置替换；输出布局或导出选项不同则需要新适配。ONNX 是交换格式，不意味着所有模型共享同一后处理。算法准确率仍需要数据与参考结果验证。

### 9.3 增加 MQTT 物联网输出

检测模型继续生成同一个权威结果，分发器新增 MQTT 实例，按照产线或工位主题发布。相机与推理无需学习 MQTT。

MQTT 断开时，是否暂存、重试或丢弃由该通道策略决定，不让整个检测流程同步等待消息服务器。协议确认与下游业务已处理仍需分别表达。

### 9.4 远程 HTTP 看板卡住

输出插件在独立进程，每个客户端有缓冲和并发上限。客户端不读取时可以断开；输出进程卡死可以单独重启，其他输出继续工作。

外部前端由 Java/Web 团队独立部署，调用 REST 查询、订阅 SSE，并通过授权图像入口显示图片。本框架不需要把前端打包进工业控制主程序。

### 9.5 RustFS 断网

存储进程上传超时并使用有限暂存，及时释放正式检测图像缓冲。记录明确显示图像待上传或失败，恢复后按对象身份对账。

可选存图项目可能继续检测；强制留图项目可能必须暂停接纳。后者是业务上主动停止，不是程序被上传调用拖死。这两种状态必须在运维界面和日志中区分。

### 9.6 算法进程崩溃

主控保持响应，标记受影响的在途任务，通知对应产线状态，再按策略重启和预热模型。共享该检测进程的任务可能一起失败，不能声称只影响某一张图。

其他独立工位是否继续由依赖关系决定；多相机共同判定同一个产品时，缺失一项检查就会影响最终结果。

## 10. 四条架构思维在这里分别带来什么价值

| 思维 | 具体体现 | 对团队的价值 |
|---|---|---|
| 文件组织 | 设备、运行、检测模型、应用协调、输出有独立目录 | 新成员知道从哪里找代码 |
| 层次分离 | UI 调用应用用例，模型不依赖 SDK 和 HTTP | 团队可以围绕接口并行协作 |
| 职责分离 | 调度不处理 SQL，模型不直接发 PLC，输出不改判定 | 修改一个能力时影响范围可控 |
| 领域抽象 | 工件、检查项、完成条件及未完成状态明确表达 | 业务规则不会散落在设备和页面回调里 |

例如“收到两个 bool 就判定完成”在初始演示中可能够用，但增加第三个相机、发生重复消息或结果迟到后容易出错。集中表达检查项身份和完成条件，让变化有一个权威位置。

这不意味着必须引入一整套复杂 DDD 框架。简单结构体与集中函数也可以形成良好的业务模型。也不需要把每个模块都变成 DLL、每个类都分配线程。

## 11. 运行可靠性如何验证

“用了多进程”和“正常跑了一次”都不是生产可靠性证明。应该在目标配置上测试失败路径：

| 验证 | 应观察的结果 |
|---|---|
| 拔掉相机或模拟 SDK 卡死 | 主控可响应，相关任务明确失败，不沿用旧图 |
| 算法超时后又返回结果 | 迟到结果不能改写已结束任务或串入新任务 |
| 输出插件卡死、慢客户端积压 | 检测和其他输出不发生同步阻塞，缓冲有上限 |
| RustFS 慢响应或暂存满 | 上传状态准确，按配方处理，不无限持有图像 |
| PLC 应答丢失或重复 | 不盲目重发过期动作，交付不确定性明确记录 |
| 在途时切换配方 | 一个工件不会混用两套参数 |
| 长时间连续运行 | 内存、句柄、线程、队列无持续异常增长 |

模拟设备使大部分机制可在 CI 中验证；真实 SDK、触发同步、吞吐与硬件兼容仍必须实测。长稳时长和性能门槛依据现场要求制定，不能用统一的虚构数字代替。

## 12. 哪些项目适合，哪些项目可以更简单

适合这套框架的情况包括：多个相似视觉项目长期维护、多品牌设备、多算法、多输出渠道，需要持续生产，或者现场故障需要快速诊断和恢复。

如果只是一次性图片分类工具、离线演示或低频人工操作的单设备工具，清晰的单进程程序可能更合适。可以复用算法和图像模块，不必部署全部工作进程。

语言选择也不是稳定性的保证。Java 可以开发工业应用，C++/Qt 也可能写出不稳定程序。本项目选择 C++/Qt 是既定方向，并有原生 SDK 集成、图像与桌面生态方面的实际便利；对应也要承担内存管理、ABI 和原生依赖维护成本。

普通 Windows/Qt 应用不承诺硬实时。精确触发和剔除时序通常交给 PLC 或硬件，紧急停止等功能不应依赖 HTTP 看板或桌面窗口是否正常响应。

## 13. 开发和协作时应形成的共识

1. 一次检测的正确性包含结果正确、工件关联正确和时间有效，不能只看模型输出。
2. 执行失败、质量 NG、输出失败是不同状态，记录和处理不能混用。
3. 设备或网络有可能卡住，所有关键等待和积压都必须有边界。
4. 插件提供扩展，进程提供隔离，两者不能互相替代。
5. 软件其他部分仍能运行，不代表生产业务一定允许继续。
6. 统一框架复用机制和测试，但不替代具体设备、模型和现场验收。
7. 新需求先判断属于运行机制、检测规则、应用流程还是外围适配，再决定代码位置。
8. 接口、诊断与演示是框架的一部分，不能只交付一组难以使用的抽象类。

具体的进程划分、目录、插件契约和阶段验收以配套《架构设计》为准；本文用于帮助参与者理解这些设计为何存在，以及它们带来的收益和代价。
